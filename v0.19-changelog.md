# mofei-dsh v0.19.0 变更说明（DSH 工作区联动 + 官方会话侧栏）

## 功能

### 新项目默认保存到当前 DSH 工作区

- 墨扉读取当前 DSH 会话的 `cwd`，将其作为新小说项目的默认 `rootDir`。
- 初始向导会显示当前工作区路径；作者仍可通过“选择文件夹”改用专用小说目录。
- 从项目列表新增小说时也采用同一默认路径，避免只有首次向导才正确落盘。
- 未选择 DSH 工作区时保留原有默认 `.mofei/projects/<project-id>` 存储行为。

### 会话选择统一使用官方 DSH 侧栏

- Web 变形态继续将官方 DSH 侧栏放在右侧。
- 点击官方侧栏的原生展开按钮不再关闭墨扉；侧栏扩展后显示原生工作区和历史会话树。
- 墨扉工作台会随侧栏宽度协调缩放，避免覆盖会话列表。
- Web 模式移除墨扉顶栏“会话”按钮和重复历史会话菜单；独立工作台模式保持原有菜单能力。

### 快捷操作面板交互修复

- 顶栏三点按钮改为可切换，再次点击即可收起面板。
- 面板新增明确的关闭按钮，并支持 `Escape` 和点击外部关闭。
- 面板标题和操作名称改为作者可理解的中文文案，隐藏 `/mofei:*` 内部命令格式。
- 移除重复的摘要入口，保留新建、摘要、风格、检索、版本历史、后台任务、写作会话等常用操作。

## 验证

- `node --check plugin/src/client/legacy.js`
- `node plugin/esbuild.config.js`
- `node tools/verify-client-bundle.mjs`
- `node test-host.mjs`：`ALL 49 PASS`。
- `verify-v0.18-onboard.cjs`：验证工作区落盘、官方侧栏、Composer 边界和 500px 窄屏。
- 浏览器实测：1920px 右栏展开时，Composer 左边界与墨扉面板右边界仅保留 1px 分隔线，标题、工作区行和输入框完整可见。
- 浏览器实测：快捷操作面板可通过关闭按钮、三点按钮、`Escape` 和点击外部四种方式收起。

## 布局修复

展开官方侧栏时，DSH 的 `centerCol` 已经从网格中扣除了侧栏宽度。旧 CSS 在 Composer 根节点再次扣除同一宽度，导致 Composer 向左偏移约 280px，被墨扉面板覆盖。现在只预留 Composer 自身宽度：

```css
padding-left: calc(100% - var(--mf-dsh-composer));
```
