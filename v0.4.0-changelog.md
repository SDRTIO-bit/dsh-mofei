# dsh-mofei v0.4.0 变更说明（改码完成，待重启验证）

> 基线：v0.3.1（AI 会话历史/批量摘要/17 skills）。
> 数据兼容：无数据结构变更；`.mofei-stats.json` 的 `days` 原本就有，仅 `stats` RPC 多返回 `calendar` 视图。

## 新增

1. **Markdown 工具条（编辑器）**
   - H2 / H3 / 加粗 / 斜体 / 无序列表 / 引用 / 行内代码 / 代码块 / 分隔线。
   - 支持选中文本或光标所在整行；插入后自动定位选区，正文状态切为未保存。
2. **章节/卷拖拽排序**
   - HTML5 原生拖拽：拖动章节或卷到目标上方/下方，按半高判定前后。
   - Host RPC：`reorder-chapters`（chapterIds 全量顺序）/ `reorder-volumes`（volumeIds 全量顺序）。
   - 校验 ID 完整、不重复；非法顺序返回 `INVALID_ORDER`。
3. **写作热力图**
   - `stats` RPC 新增 `calendar`（'YYYY-MM-DD' → chars）。
   - Client 页脚「写作热力图」按钮：最近 84 天（12 列）热力格，四档颜色，悬停显示日期与字数。
4. **Agent 工具 16 → 18 个**
   - 新增 `mofei_reorder-chapters` / `mofei_reorder-volumes`。

## 改动文件

```text
plugin/lib/index.js    stats calendar + reorder-chapters/volumes + 18 工具
plugin/lib/client.js   Markdown 工具条 + 拖拽排序 + 写作热力图
plugin/package.json    v0.4.0
verify-v7.cjs          新增 UI 回归（工具条/热力图/拖拽/AI 面板）
test-host.mjs          14 项（新增 reorder 集成测试）
```

## 验证清单（重启后）

- [ ] `test-host` 14/14（reorder 顺序与非法 ID）
- [ ] `verify-v7.cjs` ALL PASS（工具条/热力图/拖拽/AI 面板）
- [ ] 手动：打开章节 → 选中文字 → 点 B → 出现 `**文字**`
- [ ] 手动：拖拽第二章到第一章上方 → 刷新后顺序保持
- [ ] 手动：底部「写作热力图」显示 84 格
- [ ] `Tool.listTools` 可见 18 个 `mofei_*`

## 回退

- 拖拽/RPC 均为增量：注释 `reorder-*` handlers 与两个工具定义即可回到 v0.3.1。
- Markdown 工具条与热力图只存在于 client，删对应 JSX/CSS 即可。
