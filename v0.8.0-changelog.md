# dsh-mofei v0.8.0 变更说明（@提及桥接 / 实体快照回滚 / 流水线冒烟）

> 生成：2026-08-15。基线：v0.7.0 全绿。子代理 A/B 并行交付 + 主代理集成。
> Host 改动需重启 DSH 生效；client 改动刷新即生效。

## 新增

1. **@提及桥接（工作包 B + 主集成）**
   - 编辑器头部新增「送章 / 送选中」按钮，生成结构化提及文案：
     `【墨扉 · 项目《X》 · 章节《Y》】` + projectId/chapterId + 内容摘录（≤4000 码点）+
     工具指引（`mofei_read-chapter`）。
   - 真桥接：client `apply(ctx)` 捕获 `ctx.get('sessions')`，通过
     `sessions.list.getSnapshot().current → sessions.binding(id).session.prompt([...], 'queue')`
     把提及直接送进当前 DSH 会话（RpcResult `{ok:true,value:{accepted:true}}`）。
   - 降级：无 sessions 服务 / 无当前会话 / prompt 失败时自动 `navigator.clipboard` 复制，
     编辑器头部显示蓝色提示（4 秒自动消失）。
   - 纯函数 `agent-bridge.js`：truncateMention / buildChapterMention / buildSelectionMention /
     buildRangeMention（26 项单测）。

2. **角色 / 笔记 / 世界书快照回滚（工作包 A）**
   - 实体级 `history[]` 惰性创建（不升级数据版本），每次真正修改前 push 快照，上限 50 条，
     revision 单调递增（`historySeq`），旧数据无 history 视为空。
   - 快照字段：character（name/description/isFavorited）、note（title/content/categoryId/isLocked/isHidden）、
     world-entry（name/keys/content/isEnabled/constant/order）。
   - 接入点：update-character / update-note / move-note / update-world-entry / move-world-entry /
     update-world-entries（批量 patch，仅实际变化的实体）。
   - 新 RPC：`entity-history`（最新在前）、`rollback-entity`（回滚前先 push 当前值，产生新版本）。
     错误码：PROJECT_NOT_FOUND / INVALID_KIND / ENTITY_NOT_FOUND / REVISION_NOT_FOUND。
   - UI：角色/笔记/世界书编辑器头部「历史」按钮 → 历史列表 + 两击确认回滚；回滚后本地编辑态同步。
   - test-host 新增 7 项，共 **39/39**。

3. **子代理写作流水线**
   - 新增 `tools/verify-writing-pipeline.mjs`：真实 RPC 模拟 Writer → Reviewer → 冲突保护 → 合并提交
     （数据路径冒烟，当前已 PASS）。
   - 真子代理（subagent 调用 `mofei_*` 工具）流水线实测：需 DSH 重启加载 v0.5 工具 schema 修复后执行
     （AGENTS.md 已有 Writer/Reviewer 提示词模板）。

## 改动文件

```text
plugin/lib/index.js                  实体快照/历史/回滚 RPC（entity-history / rollback-entity）
plugin/src/client/agent-bridge.js    新增：@提及文案纯函数
plugin/src/client/agent-bridge.test.mjs 新增：26 项
plugin/src/client/legacy.js          集成送章/送选中桥接 + 实体历史/回滚 UI
plugin/lib/client.js                 esbuild 重新打包产物
test-host.mjs                        39 项（新增实体快照回滚 7 项）
tools/verify-writing-pipeline.mjs    新增：Writer→Reviewer 数据路径冒烟
verify-v0.8.cjs                      新增：v0.8 浏览器验收
plugin/package.json                  version 0.8.0
```

## 验证结果（全绿）

```text
自动化：test-txt 9 · world 7 · ai 7 · summary 10 · grid 22 · page 23 · layout 19 ·
        limits 16 · summary-panel 40 · worldbook 21 · agent-bridge 26 · test-host 39
        + writing-pipeline ALL PASS + client 契约 OK
浏览器：verify-v0.6 / v0.7 / v7 / v5-ui / v0.8 全部 ALL PASS
        （v0.8 实体回滚 UI 在旧 Host 上按预期 SKIP，重启后跑完整验证）
```

## 待重启清单（累计）

- v0.5：5 个 Agent 工具 output schema 修复。
- v0.7：chapter-summaries / 摘要 SSE / rangeIds / force / 世界书批量。
- v0.8：entity-history / rollback-entity。
- 重启后：`node verify-v0.7.cjs`、`node verify-v0.8.cjs`、真实子代理 Writer→Reviewer 流水线实测。
