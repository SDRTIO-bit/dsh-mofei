# dsh-mofei v0.10.1 变更说明（文件优先闭环 + 结构化检索 + 风格补全 + Studio VSCode 化）

> 生成：2026-08。基线 v0.10.0。
> 定义不变：墨扉 = MOFEI-SPEC（文件规范）+ MOFEI-AGENT（写作 preset/技能/工具）+ MOFEI-STUDIO（写作工作台）。

## 新增 / 变更

### 1. P0 文件树成为唯一事实源

- `plugin/lib/index.js` 新增 `import-file-tree()`：扫描 `.mofei/projects/**`，解析 frontmatter + Markdown 正文重建 store。
  - 冲突规则：文件 revision ≥ 内存 revision → 文件胜出；否则内存胜出并计入 conflicts。
  - 外部新建的章节/角色/笔记/世界书/摘要/链/项目自动吸收；`nextId` 防 id 冲突。
- `load()` 启动时检测 `.mofei/projects` 存在 → 自动导入（文件树优先），导入有变更再落盘 JSON。
- 新增 RPC：`reload-from-files`（手动重载）、`file-tree-status`（每实体文件/内存同步状态：synced / diverge-file / diverge-store / missing-file）、`read-chapter`。
- 新增 `tools/migrate-to-files.mjs`：旧 JSON 全量导出为文件树 + 双向校验（支持 `--verify-only`），写 `migration-manifest.json`。
- 修复 v0.10.0 潜伏 bug：`mirrorFileTree` 读 `chainStore.projects`（应为 `byProject`）→ **链文件此前从未镜像到磁盘**，现已正确写入 `chains/*.md`。
- RPC payload 上限 2MB → 16MB（真实整本书 TXT 导入需要）。

### 2. P1 结构化检索（RAG 雏形）

- 新增 `retrieve` RPC：轻量纯 JS 倒排索引（中文 bigram + 单字 + 拉丁词），按项目缓存、实体签名失效（章节 revision+标题哈希，其余内容哈希）。
- 覆盖实体：章节 / 角色 / 笔记 / 世界书 / 章节摘要；实体名称/标题入索引。
- 返回结构：`{ entityType, entityId, title, line, snippet, score }` + `query/total/tookMs`。
- `mofei_retrieve` 工具改接 `retrieve`（接口预留，未来可换 DSH RAG 生态）。
- 性能实测：百万字项目（100 章 × 1 万字）冷查询含建索引 151ms，热查询 1ms。

### 3. P1 写作风格补全

- 风格注入全部写作路径：`ai-assist`（续写/改写/自定义，SSE 流同）、`run-prompt-chain`（系统提示 + 新增 `{{style}}` 宏，prompt-chain.js MACROS 扩展）、`chapter-context`。
- 项目级 `styles/` 覆盖全局：`readStyle(styleId, projectId)` 先查项目目录再查全局。
- 风格 CRUD：`save-style`（scope: global/project）、`delete-style`；`get-style` / `set-project-style` 补齐校验与错误码（`STYLE_NOT_FOUND` / `STYLE_REQUIRED`）。
- `list-styles` 支持项目级合并列表（项目级优先，同 id 去重），条目含 description/tags/scope。

### 4. P2 MOFEI-STUDIO VSCode 化

- 左侧 48px 垂直 Activity Bar（9 视图）：项目 / 检索 / 角色 / 世界 / 笔记 / 摘要 / 链 / 风格 / 记录；当前视图高亮，图标 + 悬浮 label。
- 编辑器标签增强：章节 + 笔记混开（章/笔标记）、右键菜单（固定📌 / 关闭其他 / 关闭）、拖拽排序、滚动位置记忆（localStorage `mofei.scroll.<project>.<chapter>`）。
- 新增「检索」视图：跨实体检索，命中展示实体类型/行号/snippet/score，点击跳转实体。
- 新增「风格」视图：风格文件列表（✓ 标记当前项目风格）+ 编辑器（名称/描述/标签/正文 + 预览 + 保存/删除，项目级优先）。
- 命令面板补齐：`/mofei:new-chapter` `/mofei:writer` `/mofei:reviewer` `/mofei:summary` `/mofei:style` `/mofei:retrieve` `/mofei:git-history`。
- Writer/Reviewer 快捷指令：`agent-bridge.js` 新增 `buildWriterMention` / `buildReviewerMention`（任务头 + 整章提及），经 @提及桥接发给当前 DSH 会话。
- 专注模式与移动端适配 Activity Bar（隐藏中栏/手柄）。

### 5. P2 Prompt Chains git 版本 diff

- 新增 `git-history` RPC：`{projectId}` → 项目目录提交史；`{projectId, chainId}` → 链文件 `git log -p` diff（最近 3 条带 patch）。非 git 工作区返回 `available:false` 优雅降级。
- 链保存自动 git 提交（`gitCommitAll`，仅 add .mofei 后 commit，无变更跳过）。
- 修复 Windows 中文 commit message 损坏：`-m` 参数改临时 UTF-8 文件 + `-F`（此前中文名变 `??`）。
- 工作区已 `git init` 且 `.gitignore` 仅跟踪 `.mofei/`（`*` + `!.gitignore` + `!.mofei/` + `!.mofei/**`），已有初始提交。
- Studio 链面板新增「历史/对比」按钮；命令面板 `/mofei:git-history` 打开历史面板（提交列表 + diff）。

### 6. OpenFic 调研

- 新增 `docs/OPENFIC-RESEARCH.md`：OpenFic 摘要（两级+固定窗聚合+签名链）与检索（chunk 级+RRF 混合+增量索引）设计要点与墨扉落地对照；明确不移植 LanceDB/LangChain/langgraph。

## 验证结果

```text
test-host                         46/46
verify-spec                       文件树回读/冲突保护/外部新建/status ALL PASS
verify-retrieve-style             结构化命中 5 实体 + 百万字冷 151ms/热 1ms + 风格注入/覆盖/CRUD ALL PASS
verify-git-history                链自动提交 ×2 + 中文信息 + diff ALL PASS
agent-bridge.test                29/29
verify-agent-isolation/tools/surface  ALL PASS
verify-v0.10-workbench            活动栏 9 视图 + 混开标签 + 固定 + 检索视图 + 风格视图 + 命令面板 ALL PASS
verify-v0.10-isolation + 旧六项（v0.6/v0.7/v0.8/v0.9/v7/v5-ui）  浏览器回归 ALL PASS
```

## 改动文件

```text
plugin/lib/index.js               文件树导入/重载/状态 RPC + retrieve 索引 + 风格 CRUD/注入 + git 适配 + 链镜像修复
plugin/lib/tools.js               mofei_retrieve 结构化 schema
plugin/lib/prompt-chain.js        {{style}} 宏
plugin/lib/client.js              重建的 client bundle
plugin/src/client/legacy.js       活动栏/混开标签/检索视图/风格视图/命令面板/git 面板
plugin/src/client/prompt-chains.js 「历史/对比」按钮
plugin/src/client/agent-bridge.js Writer/Reviewer 提及构建
plugin/src/client/agent-bridge.test.mjs  3 项新单测
tools/migrate-to-files.mjs        新增：JSON→文件树迁移
tools/verify-retrieve-style.mjs   新增：检索+风格验收
tools/verify-git-history.mjs      新增：链 git diff 验收
tools/verify-spec.mjs             扩展文件树回读验收
verify-v0.10-workbench.cjs        扩展活动栏/混开标签/检索/风格/命令面板验收
verify-v0.6/v0.7/v0.8/v0.9/v7/v5-ui.cjs  横向 .mf-tab 改 Activity Bar 选择器
docs/OPENFIC-RESEARCH.md          新增：OpenFic 调研
```

## 备注

- 工作区 `OpenFic-DSH` 已初始化 git 仓库（仅跟踪 `.mofei/`），链保存自动提交；章节等其他实体写操作自动提交留待 P3 git 适配器。
- 验收脚本统一用 Windows node/PowerShell 跑，3088 为 novel profile 进程。
