# Dependencies and regenerated release packages
node_modules/
**/node_modules/
/plugin/dist/

# Local tooling, logs, and transient diagnostics
/.codegraph/
/.omo/
/.jez/
/.playwright-cli/
/output/
/browser-screenshots/
*.log
*.tmp
/probe-*.cjs
/verify-shots/*/
/verify-shots/*.png
/verify-shots/*.json
/verify-shots/*sample*.txt

# Runtime state belongs to the local DSH instance, not source history.
/.mofei-*.json
/.openfic-*.json

# Keep source, presets, skills, scripts, tests, and documentation tracked.
# 小说写作数据(.mofei/)属于私人内容，不入库——由本地 DSH 实例持有。

# 小说写作数据（私人内容，不入库）
/.mofei/

# 历史交接/规划文档（含本机开发痕迹，仅本地保留，不入库）
/docs/archive/
/HANDOFF.md
/NEXT-SESSION.md

# 本地拆书研究样本（网文文本，非源码不入库）
/新建文件夹/
