# mofei-dsh v0.7.0 变更说明（摘要维护面板 / 世界书搜索与批量）

> 生成：2026-08-15。基线：v0.6.0（自动化 + 三浏览器回归全绿）。
> 本批范围：GAP-ANALYSIS v0.7。子代理 A（Host）/ B（摘要面板）/ C（世界书工具）+ 主代理集成验证。

## 新增

1. **摘要维护面板（编辑器头部「摘要」按钮）**
   - 三个 tab：`章节` / `区间` / `生成`。
   - 章节 tab：按 order 列表显示每章「无 / 过期 / 有」badge、摘要预览、单章「重算」
     （强制重新生成，不再跳过未过期章）；顶部统计「共 N 章 · 已有摘要 X · 过期 Y」。
   - 区间 tab：区间摘要列表 + 单区间强制重算。
   - 生成 tab：批量生成过期章节摘要 / 过期区间摘要；进度条 + `done/total` + 结果统计。
   - 数据源：新增 RPC `chapter-summaries`（批量章摘要）；SSE
     `POST /api/mofei/stream/ai-summarize`（`progress` → `done`/`error`，断连安全）。
   - 兼容：旧 Host（重启前）自动回退到逐个 `chapter-summary` 与非流式 RPC，面板仍可用。

2. **摘要 RPC/SSE 增强**
   - `ai-summarize-chapters` / `ai-summarize-ranges` 支持 `force: true`（跳过过期判断，强制重算）。
   - `ai-summarize-ranges` 支持 `rangeIds`（只重算指定区间；未命中返回 `RANGE_NOT_FOUND`）。
   - SSE 路由同时注册 `/api/openfic/stream/ai-summarize` 旧名别名。

3. **世界书搜索与批量**
   - 世界书 tab 顶部搜索框：按名称 / 触发词大小写不敏感过滤。
   - 每行 checkbox + 全选/反选；批量栏显示「已选 N 项」，批量启用 / 禁用 / 删除（删除两击确认，3s 超时）。
   - 新 RPC：`update-world-entries`（批量 patch isEnabled/constant）、`delete-world-entries`（批量删除并重排 order）。
   - 旧 Host 回退：逐条调 `update-world-entry` / `delete-world-entry`。
   - 名称唯一性：`create-world-entry` / `update-world-entry` 拒绝重名（`DUPLICATE_WORLD_NAME`，
     大小写与空白不敏感）；客户端在提交前预检提示。

## 改动文件

```text
plugin/lib/index.js                  chapter-summaries RPC + 摘要 SSE(streamSummarize) + rangeIds/force
                                     + update/delete-world-entries + 世界书名唯一性
plugin/src/client/summary-panel.js   新增：摘要面板组件 + 纯函数 + CSS
plugin/src/client/summary-panel.test.mjs 新增：40 项
plugin/src/client/worldbook-tools.js 新增：世界书搜索/选择/批量计划纯函数
plugin/src/client/worldbook-tools.test.mjs 新增：21 项
plugin/src/client/legacy.js          集成 SummaryPanel（SSE 消费 + 旧 Host 回退）+ 世界书批量 UI
plugin/lib/client.js                 esbuild 重新打包产物
test-host.mjs                        32 项（新增摘要流/世界书批量/唯一性/force 重算 9 项）
verify-v0.7.cjs                      新增：v0.7 浏览器验收
plugin/package.json                  version 0.7.0
```

## 验证结果（全绿）

```text
node --check：lib/index.js / legacy.js / summary-panel.js / worldbook-tools.js  OK
client 契约：CLIENT BUNDLE CONTRACT OK
test-txt 9/9 · test-world 7/7 · test-ai 7/7 · test-summary 10/10
project-grid 22/22 · project-page 23/23 · layout 19/19 · editor-limits 16/16
summary-panel 40/40 · worldbook-tools 21/21
test-host 32/32（含 SSE progress/done、rangeIds、force、世界书批量、唯一性）
浏览器：verify-v0.7 ALL PASS（旧 Host 回退路径实测；新 SSE/force 路径待重启后同脚本复跑）
        verify-v0.6 / verify-v7 / verify-v5-ui ALL PASS（无回归）
```

## 生效说明

- client 侧即时生效（esbuild 后刷新）。
- **Host 新 RPC/SSE/force/批量接口需重启 DSH 生效**；重启后连同 v0.5 的 5 个工具 schema 修复一起上线。
- 重启后建议重跑 `node verify-v0.7.cjs` 覆盖新流式路径。
