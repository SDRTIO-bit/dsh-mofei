# dsh-mofei v0.16.0 变更说明（墨菲子代理辅助：subagent_with_model）

> 生成：2026-08-16。基线 v0.15.0（commit `43105fe`）。
> 需求：墨菲（mofei-writer 写作会话）派生子代理时，由墨菲自主指定**模型 / 推理强度 / 装配上下文**。
> 计划与方案：`docs/archive/PLAN-v0.16-subagent-max.md`。

## 调研结论（摘要）

- DSH 底层（dsh-subagent → dsh-agent → dsh-llm → llm-deepseek）完整支持每子代理覆盖
  `provider / model / maxTokens / reasoningEffort`（`off|high|max` → wire `reasoning_effort`），
  但官方 `subagent`/`subagent_fork` 工具的参数 schema 不暴露这些字段；
- npm 生态两个候选：`@aaravarr/dsh-subagent-max`（新增工具，零冲突）与
  `dsh-delegate-router`（包装官方工具，侵入墨菲流水线）——采用前者并本地增强。

## 功能

### vendor 增强包 `vendor/dsh-subagent-max/`（基于官方 v0.1.1，git 管理，link 挂载）

`subagent_with_model` 工具新增两个参数（原版已有 model/provider）：

| 参数 | 说明 |
| --- | --- |
| `model` / `provider`（原版） | 每次调用指定子代理模型与 provider 路由 |
| `effort`（新增） | `off \| high \| max`，子代理推理强度 → `agentOptions.reasoningEffort` |
| `context`（新增） | 主模型手动装配的上下文，注入子代理 prompt 前置（`【主模型装配的上下文】…【任务】…`） |

### novel profile 装配（3088）

- `~/.dsh/profiles/novel/package.json`：`link:` 依赖 → `vendor/dsh-subagent-max/`；
- `~/.dsh/profiles/novel/cordis.patch.yml`：`dsh-subagent-max` entry（host 工具 + client 查看器 UI）；
- vendor 包 `node_modules` junction → `~/.dsh/profiles/node_modules`（link 包 peer 依赖解析）。

### 墨菲集成（presets 两处同步：项目副本 + `~/.dsh/.agent-presets`）

- `mofei-writer` 白名单新增 `tool-subagent-with-model`（spawn / continuable / maxDepth 3）；
- persona 新增【子代理装配】指导：先 `mofei_get-chapter-context` 取精装上下文放入 `context`，
  按任务轻重指定 `model`（pro/flash）与 `effort`（high/max 深度创作审稿，off/high 轻任务）；
- 官方 `subagent`/`subagent_fork` 原样保留（Writer→Reviewer 流水线不动）；
- standard（coding）会话不可见（preset 白名单隔离，双向零污染）。

## 验证结果（全绿）

```text
verify-v0.16-subagent.cjs  ALL PASS：
  写作会话（mofei-writer）工具清单含 subagent_with_model
  standard 会话无 subagent_with_model（隔离成立）
  真实回合：墨菲按要求调用 subagent_with_model，
    工具帧参数 = model=deepseek-v4-flash + effort=off + context=测试上下文（全部正确）
  子代理回合完成并回传
```

## 改动文件

```text
vendor/dsh-subagent-max/            新增：官方 v0.1.1 解包 + effort/context 增强 + README 说明
~/.dsh/profiles/novel/package.json   link 依赖（runtime 侧）
~/.dsh/profiles/novel/cordis.patch.yml  dsh-subagent-max entry（runtime 侧）
presets/mofei-writer/agent.cordis.yml  白名单 + persona 子代理装配指导
~/.dsh/.agent-presets/mofei-writer/agent.cordis.yml  同上（runtime 副本）
docs/archive/PLAN-v0.16-subagent-max.md          计划与验收文档
v0.16-changelog.md                  本文档
verify-v0.16-subagent.cjs           端到端回归（工具可见性/隔离/真实调用参数）
```

## 备注

- 子代理查看器 UI（多面板实时流式）随 client 半区自动装配，打开 3088 页面即生效；
- `dsh-delegate-router` 未安装：它会包装官方 subagent/subagent_fork，侵入墨菲 Writer→Reviewer 流水线；
- 回滚：移除 profile 依赖 + patch entry + preset 白名单三处，重启即还原。

---

## v0.16.1（打包修订，2026-08-16）

用户决定：subagent_with_model **直接并进 dsh-mofei 插件本体**，不再作为独立第三方包挂载。

- 新增 `plugin/lib/subagent-max.js`（`dsh-mofei/subagent-max` 子模块）：零外部依赖
  （工具定义 / settleRun / maxDepth 校验内联，行为与官方 dsh-subagent 一致）；
- `plugin/package.json`：exports 加 `./subagent-max`、files 加 `lib/subagent-max.js`；
- novel profile：移除 `@aaravarr/dsh-subagent-max` link 依赖与 junction；
  `cordis.patch.yml` entry 改为 `dsh-mofei/subagent-max`；
- preset 白名单 name 改为 `dsh-mofei/subagent-max`（两处同步）；删除 `vendor/` 目录；
- 修复：手写工具 parameters 必须用标准 OpenAI JSON Schema（属性级 `required` 被 DeepSeek API 拒绝，
  报 `Invalid schema for function 'subagent_with_model'`）——required 收敛为顶层数组；
- 验证：`verify-v0.16-subagent.cjs` 仍 ALL PASS（写作会话可见 / standard 隔离 / 真实调用参数正确 / 子代理回传）。
- 换机即用：安装 dsh-mofei 即自带该能力，无额外依赖安装步骤。
