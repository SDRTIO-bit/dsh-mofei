# mofei-dsh v0.6.0 变更说明（三栏调宽 / 项目宽幅页 / 编辑器内容上限）

> 生成：2026-08-15。基线：v0.5.0（已重启并双浏览器回归通过）。
> 本批范围：GAP-ANALYSIS v0.6 三件套，子代理工作包 A/B/C 并行交付 + 主代理集成验证。

## 新增

1. **三栏可拖拽调宽 + 布局持久化（工作包 A）**
   - `.mf-body` 列宽改为 CSS 变量 `--mf-left / --mf-middle` 驱动，5 列结构：
     左栏 / 6px gutter / 中栏 / 6px gutter / 编辑器。
   - 两根 `.mf-gutter` 支持 pointer 拖拽（setPointerCapture），拖动中 `.mf-body.resizing` 禁选文本；
     双击复位对应轴默认宽（左 210 / 中 250）。
   - 持久化 `localStorage['mofei.layout']`；加载时脏数据安全（clamp + 组合超宽按比例压缩 + 编辑器保底 320px）。
   - focus 模式与 <760px 移动端保持原行为（gutter 隐藏；中栏选择器因新增 gutter 调整为 nth-child(3)）。
   - 新增纯逻辑 `plugin/src/client/layout.js` + `layout.test.mjs`（19 项）。

2. **项目宽幅页 + 简介编辑（工作包 B）**
   - 项目 tab 头部新增「宽幅」按钮；开启后编辑器区整体替换为 `ProjectPage`。
   - 宽幅页 = 项目网格（宽容器下自然多列）+ 选中项目详情卡：
     封面/标题/章节数 + 简介 textarea（≤500 码点）+「保存简介」。
   - 简介走 `update-project` RPC 落盘，直接参与项目网格的标题/简介模糊搜索。
   - 点开章节自动退出宽幅页回到编辑器。
   - 新增 `plugin/src/client/project-page.js` + `project-page.test.mjs`（23 项）；
     `project-grid.js` 仅新增 `export resolveReact`。

3. **编辑器内容上限（工作包 C）**
   - 常量与原版 OpenFic 一致：`MAX_EDITOR_CONTENT_CHARACTERS = 100_000`（Unicode 码点）、
     `MAX_EDITOR_CONTENT_LINES = 2_000`；行分隔符与 CRLF 规则逐字符同原版。
   - 保存（手动 Ctrl+S 与 3s 自动保存统一走 `saveChapter`）前校验：超限 → status error +
     中文提示「正文超出上限：当前 N 字 / 100000 字，N 行 / 2000 行。请拆分章节后再保存。」，
     **不调用 RPC、不覆盖远端正文**；草稿仍按 800ms 持久化，修改回合规范围后自动保存恢复。
   - 新增 `plugin/src/client/editor-limits.js` + `editor-limits.test.mjs`（16 项）。

## 改动文件

```text
plugin/src/client/legacy.js          集成三栏 gutter + ProjectPage 宽幅 + saveChapter 上限拦截
plugin/src/client/layout.js          新增：布局纯逻辑（normalize/next/load/save）
plugin/src/client/layout.test.mjs    新增：19 项
plugin/src/client/project-page.js    新增：项目宽幅页组件 + 简介纯函数 + CSS
plugin/src/client/project-page.test.mjs 新增：23 项
plugin/src/client/project-grid.js    仅 export resolveReact
plugin/src/client/editor-limits.js   新增：内容上限纯逻辑（Apache-2.0，源自 OpenFic）
plugin/src/client/editor-limits.test.mjs 新增：16 项
tools/verify-client-bundle.mjs       knownExternal 增加 setPointerCapture
verify-v0.6.cjs                      新增：v0.6 浏览器验收
plugin/lib/client.js                 esbuild 重新打包产物
plugin/package.json                  version 0.6.0
```

## 验证结果（全绿）

```text
node --check：lib/index.js / legacy.js / project-grid.js / project-page.js / layout.js / editor-limits.js  OK
client 契约：CLIENT BUNDLE CONTRACT OK
test-txt 9/9 · test-world 7/7 · test-ai 7/7 · test-summary 10/10
project-grid 22/22 · project-page 23/23 · layout 19/19 · editor-limits 16/16
test-host 23/23
verify-v0.6.cjs ALL PASS（宽幅页/简介保存/拖拽+持久化+双击复位/上限拦截+恢复）
verify-v7.cjs ALL PASS · verify-v5-ui.cjs ALL PASS（无回归）
```

## 生效与已知状态

- 本批全部为 client 改动：esbuild 后刷新页面即生效，无需重启 DSH。
- 上批遗留：lib/index.js 的 5 个工具 output schema 修复仍需重启 DSH 一次。
