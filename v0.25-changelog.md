# mofei-dsh v0.25.0 变更说明（内置角色体系 + 子代理装配重构 + 提示词研究落地）

> 2026-08-20。本版起：子代理角色不再是项目可写死的私有列表，而是「四个内置角色（唯一事实来源）
> + 项目同 id 覆盖 + 项目自建角色」的有效角色目录；子代理由 subagent-max 显式挂载 DSH 内置
> `minimal` preset，不再依赖本机私有 `minimal-v3`。写作提示词按拆书样本与规范库研究结果重写。

## P0：内置角色体系（roles.js）

- `MOFEI_BUILTIN_ROLES` 冻结常量 = writer / reviewer / analyzer / polisher 四个内置角色，
  含默认 persona 与 effort（均 high），是运行时与前端共同使用的唯一事实来源。
- 项目文件（`.mofei-roles.json`）只保存同 id 覆盖项与自建角色：默认提示词升级后，
  未覆盖的项目自动使用新版本；覆盖项完整替换内置角色同名条目，自建角色排在内置之后。
- `mergeEffectiveRoles(overrides)` 在读取层合并：内置角色（未覆盖）→ 项目覆盖（isOverridden）
  → 自定义角色；输入重新规范化，返回值不引用或修改持久化数组。
- 删除语义修正：删除内置角色 = 清除项目覆盖并恢复默认（`resetToBuiltin: true`）；
  删除自建角色 = 真正删除。RPC 返回 `role` 详情与 `resetToBuiltin` 标记。
- 角色解析支持名称别名：`subagent_with_model` 的 `role` 参数先按 id 读取，失败后
  用 `listRoles` 按名称匹配（大小写不敏感）。
- `tool.mofei_list/read/write/delete-role` 口径改为「实际生效角色」：列表与详情带
  `source`（builtin/project）、`isBuiltin`、`isOverridden`、`canReset`。

## P0：子代理装配重构（subagent-max.js）

- 子代理 preset 由 `childPreset` 配置决定（缺省 `minimal`）；配置后本模块的 one-shot
  provider 显式挂载该 preset，因为官方 continuable provider 会固定 `composeFrom(parent)`，
  无法切换到另一个 agent preset。`childProviderName` 可改挂载的 provider 名。
- `roleCatalog: mofei-writing` 时自动并入 `builtinRoleConfig()`，配置里的 `roles` 只作补充。
- 委托提示词分离「项目上下文（事实资料）」与「本次任务（唯一执行目标）」两个区块，
  避免长章节上下文被当作额外指令、子代理开始复述编排流程。
- 子代理结果从会话事件还原：`assistant/message` 文本输出 + `turn/end` reason 映射
  stopReason（completed / max-tokens / refusal / aborted / error）。
- 支持 `inheritParentContext`；`childDepthOf` 从父会话 `delegationDepth` 递增，防循环派单。

## P1：行为修正

- `resolvedModel`：项目级 general 不再遮蔽全局 general 的 `dedicated` 判定；
  项目专用角色与全局专用角色分层解析，`source` 区分 role/general/dsh-default。
- `delete-project`：带 `rootDir`（外部目录）的项目只删除记录，绝不递归删除实体目录，
  避免删除整个小说文件夹或用户选定的上级目录；默认镜像项目才清理 `.mofei/projects/<id>`。
- 角色面板（roles-panel.js / agent-models-panel.js / settings-panel.js / legacy.js）
  同步展示有效角色目录、来源与覆盖状态。

## P2：连载写作提示词研究落地

- 新增 `docs/REFERENCE-PROMPT-RESEARCH-2026-08.md`：以本地 7 本拆书样本（前 30 章）
  与 388 份写作规范资料交叉验证，提炼可迁移的连载机制（状态变化、冲突四要素、铺垫兑现、
  对话功能、节奏切换、去空泛化），明确「只拆机制、不复制作品」的使用边界。
- 依研究重写 `presets/mofei-writer/agent.cordis.yml`：
  - 主会话改为「中控」人设：取上下文 → 装配事实包 → 判断编辑意图 → 派单 → 结果裁决；
  - 四个内置角色的默认 persona 落地（Writer 正文合同 / Reviewer 审稿检查项 /
    Analyzer 设定与因果核查 / Polisher 语言润色）；
  - 写作闭环：续写或改写先 Writer 再 Reviewer，实质问题定点修复后复核，最多两轮；
  - 主会话默认子代理 preset 改为 `minimal`（`childPreset: minimal`，不再引用 `minimal-v3`）。

## P3：测试与文档

- 新增 `test-roles.mjs`（内置目录/覆盖/合并/删除还原）、`test-role-host.mjs`
  （RPC 全链路 mock ctx）、`test-subagent-max-role.mjs`（角色解析与 persona 注入）、
  `test-mofei-writer-preset.mjs`（preset 内容断言：minimal 子代理、角色人设关键词、
  中控职责与边界）；扩充 `test-host.mjs`。
- `README.md` / `plugin/README.md`：独立 novel profile 安装步骤、preset 拷贝、
  子代理 minimal preset 说明（不再要求复制 `minimal-v3`）。
- `.gitignore` 排除 `新建文件夹/`（本地拆书研究样本，网文文本不入库）。

## 验证

```powershell
node --check plugin\lib\roles.js
node --check plugin\lib\subagent-max.js
node --check plugin\lib\index.js
node test-roles.mjs
node test-role-host.mjs
node test-subagent-max-role.mjs
node test-mofei-writer-preset.mjs
node test-host.mjs
node tools\verify-agent-tools.mjs
node tools\verify-agent-isolation.mjs
node tools\verify-subagent-pipeline.mjs
```

## 待办（v0.26+）

- schemastery `Config` schema（校验 + 设置 UI）——需引入 `@deepseek-ai/schemastery` 依赖
  （自 v0.24 延续）。
- 同步事件化：SSE 推送 sync 事件替代 2s 轮询（断线保留轮询兜底）。
- 子代理「写作时长记录」与「实体回滚的 Agent 操作审计」。