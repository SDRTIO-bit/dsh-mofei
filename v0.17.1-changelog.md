# dsh-mofei v0.17.1 变更说明（浏览器操控测试视觉修复）

> 生成：2026-08-16。基线 v0.17.0（commit `4ce4d31`）。
> 背景：为 agent 装配 dsh-browser（真实浏览器操控测试能力），并完成首轮真实浏览器测试；
> 通过 vision 模型审查截图发现并修复 4 处视觉问题。

## 变更

### 测试能力：dsh-browser 装配（web profile，3080）

- `vendor-tools/dsh-browser/`：官方 0.1.0 本地化（依赖 junction：playwright-core / schemastery / dsh-tools）；
- `dev_install_package` 热装配（免重启），重启后由 web profile bundles 自动装配（双路径一致）；
- 效果：agent 可用 `browser_open/click/type/screenshot/eval` 等真实操控墨菲界面，测试不再只靠 Playwright 脚本。

### 视觉修复（vision 模型审查截图驱动，全在 `plugin/src/client/legacy.js`）

| 问题 | 修复 |
| --- | --- |
| 变形态右侧头部文字重叠（"探索未至之境"+"预览版"徽标 vs "Router Standard"下拉） | headline 压缩为 15px 单行截断；heroWorkspaceRow 12px 不换行截断 + label-secondary；实测标题行与下拉行间隙 32px |
| 左下迷你导航文字对比度低（tertiary 近黑） | `.mf-mininav button` 颜色 tertiary → secondary（rgb 168,168,176） |
| 编辑器空态"选择章节后开始写作。"对比度低 | `.mf-editor-pane > .mf-empty` tertiary → secondary |
| 搜索框 placeholder 像空字段 | `input/textarea::placeholder` tertiary → secondary |

## 验证结果

```text
浏览器真实操控（dsh-browser）：
  默认态/变形/会话菜单（26 项+徽标）/技能开关/自创技能（文件落盘+DSH 自动发现）全部通过
vision-look 截图审查：
  头部重叠消除、迷你导航与 placeholder 可读、模型下拉与输入区可读（无重叠/对比度问题）
verify-v0.17-skills-sessions.cjs  回归 ALL PASS
```

## 改动文件

```text
plugin/src/client/legacy.js   变形态头部压缩 + 迷你导航/空态/placeholder 对比度（4 处 CSS）
plugin/lib/client.js          重建 bundle
vendor-tools/dsh-browser/     新增（dsh-browser 本地化包）
.gitignore                    /browser-screenshots/
v0.17.1-changelog.md          本文档
```

## 备注

- 浏览器测试截图留存 `browser-screenshots/`（已忽略，不入库）；
- vision 审查仍提示「右面板略挤」与蓝色主按钮视觉偏跳——属主观审美，暂不动。
