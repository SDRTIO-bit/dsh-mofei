# 墨扉（Mofei）× DSH 整体评估验收报告

> 验收日期：2026-08-16。基线 commit：`0248f23`（验收起始）→ `v0.18.0`（验收后提交）。
> 范围：`mofei-dsh` 插件（novel profile，3088）+ 写作预设 + 工具/技能体系 + 浏览器操控测试链路。

---

## 1. 交付版本线（v0.14 → v0.18）

| 版本 | 主题 | 状态 |
| --- | --- | --- |
| v0.14.0/0.14.1 | 变形金刚形态（原版 DSH + 墨菲气泡）、写作状态隔离 preset | ✅ 已交付 |
| v0.15.0 | 文件同步轮询（AI/外部写入 → 墨菲自动可见）+ 镜像 ping-pong 修复 | ✅ 已交付 |
| v0.16.0/0.16.1 | 墨菲子代理辅助 `subagent_with_model`（模型/推理强度/装配上下文，已并入打包） | ✅ 已交付 |
| v0.17.0/0.17.1 | 技能开关 + 自创技能入口；会话导航（全部会话/退出当前对话）；浏览器操控视觉修复 | ✅ 已交付 |
| v0.18.0 | 初始向导（小说文件夹 rootDir）；会话入口自动弹出；质感统一（令牌化） | ✅ 本轮交付 |

## 2. 本轮验收矩阵（全部执行，全部通过）

| 回归 | 覆盖 | 结果 |
| --- | --- | --- |
| verify-v0.14-view（18 项） | 默认态/变形/还原/双入口 | ✅ ALL PASS |
| verify-v0.14-chat（9 项） | 官方 composer 两态 + 联动按钮 | ✅ ALL PASS |
| verify-v0.14-writing（7 项） | 写作会话创建/入口/菜单（断言已适配 v0.17/0.18 语义） | ✅ ALL PASS |
| verify-v0.14-isolation（21 项） | 写作隔离端到端 | ✅ ALL PASS |
| verify-v0.14-workbench（7 项） | 工作台全流程 | ✅ ALL PASS |
| verify-v0.15-sync（9 项） | AI 写入/外部编辑自动可见/草稿保护 | ✅ ALL PASS |
| verify-v0.16-subagent（7 项） | subagent_with_model 可见性/隔离/真实调用参数 | ✅ ALL PASS |
| verify-v0.17-skills-sessions（10 项） | 技能开关/自创技能/会话菜单/退出对话 | ✅ ALL PASS |
| verify-v0.18-onboard（10 项） | rootDir 全链路/自动弹菜单/会话入口 | ✅ ALL PASS |
| 单元测试（12 个文件） | 工具/面板/布局/链/摘要/世界书等 | ✅ 全 PASS |
| 静态检查 | node --check 全部 lib/src + esbuild 契约 | ✅ PASS |
| vision-look 截图审查 | 变形态/头部/对比度/质感统一 | ✅ 无阻塞问题 |

## 3. 功能验收结论

- **写作主流程**：项目/章节两级导航、编辑器、自动保存、冲突保护、文件优先闭环 —— ✅
- **写作隔离**：standard 会话零墨菲工具/技能；mofei-writer 纯写作工具集 —— ✅
- **子代理辅助**：墨菲可指定 model/effort/context 派生子代理，Writer→Reviewer 流水线不破坏 —— ✅
- **技能体系**：17 内置技能开关 + 自创技能（~/.dsh/skills/，DSH 自动发现）—— ✅
- **会话导航**：墨菲内全部会话列表/切换/退出当前对话/自动弹出 —— ✅
- **初始向导**：空白状态引导选择小说文件夹，rootDir 存储全链路 —— ✅（UI 呈现待空 store 实测）
- **浏览器操控测试链路**：dsh-browser 已装配（web profile 3080），真实浏览器测试可用 —— ✅

## 4. 已知遗留（非阻塞）

1. **初始向导 UI 呈现**：需在空 store（首次使用或删除全部项目）时实测「选择文件夹」原生对话框流程；
2. **右侧面板略挤、蓝色主按钮视觉偏跳**：主观审美，暂未调整；
3. **locked 的历史测试子代理会话**：官方机制，不影响使用；
4. **rootDir 项目的 git 适配**：小说文件夹独立于工作区 git，如需版本管理可自行对该文件夹 git init（后续可做）；
5. **p2 backlog**（NEXT-SESSION.md）：检索 RRF 混合、链版本 diff、写作时长记录、Agent 操作审计。

## 5. 环境与运行

- 3088 = novel profile（墨菲写作环境），v0.18.0 运行中；
- 3080 = web profile（标准 DSH + dsh-browser + 超级模组注入器）；
- 数据：`.mofei-*.json`（缓存）+ `.mofei/projects/**`（默认存储）/ rootDir 小说文件夹（可选）。

## 6. 结论

**整体验收通过。** 全部 9 个浏览器回归 + 12 个单元测试 + 静态检查 + 视觉审查全绿；
本轮新增的初始向导（rootDir）、会话入口自动弹出、质感令牌化均验证通过。项目可继续实测与迭代。
