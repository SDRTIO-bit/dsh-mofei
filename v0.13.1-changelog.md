# mofei-dsh v0.13.1 变更说明（Bug 修复 + 实现向预览 v5 对齐）

> 生成：2026-08-16。基线 v0.13.0。
> 背景：用户反馈「有不少 bug，且实现与 HTML 预览有出入」。本版按 docs/archive/BUGFIX-HANDOFF.md §4/§5
> 实测复现并修复，将 web 模式实现逐项对齐唯一权威设计稿 `docs/archive/preview-mofei-v2.html`（v5）。

## Bug 修复

### 1. 迷你导航渲染成竖向窄条（本轮实测复现的最大视觉 bug）

- 现象：`.mf-mininav` 是 `mf-body` 的 flex 兄弟节点，被渲染成左栏与编辑器之间的
  **竖向窄条**（123×820，占满整高），5 个 tab 挤在顶部、文字裁切、下方 800px 空白；
  预览中它应是左内栏**底部横排**的迷你导航。
- 修复：把 `mf-mininav` 移入 `mf-col` 末尾（底部横条），并补
  `.mf-panel.mf-view .mf-col .mf-list{flex:1;min-height:0}` 让列表区正确填充滚动。
- 附带收益：编辑器可用宽度 721px → 850px（原先被窄条占掉的 ~155px 收回）。

## 实现向预览 v5 对齐（§4 差异清单逐项）

| # | 差异项 | 处理 |
| --- | --- | --- |
| 1 | 项目卡片（进度条 + 重命名/删除按钮 + 网格/列表切换 + 排序） | web 模式改为极简**项目行**：标题 + 「N 字 · N 章」元信息 + 选中高亮（`.mf-proj`）；重命名/删除改为 **hover 才显现**（功能不丢，静止态与预览一致）；去掉网格/列表切换与排序下拉；头部去掉「宽幅/收起」，仅剩「＋ 新建」；列表头部加「搜索项目…」输入框 |
| 2 | 章节行（卷/↑/↓/× 四个小按钮） | web 模式左栏全部行操作（`.mf-minis`）**hover 才显现**，静止态 = 标题 + rN，与预览一致（拖拽排序、上移/下移/分卷/删除功能保留） |
| 3 | 状态栏（任务/写作记录/写作热力图/保存正文 四个按钮） | web 模式状态栏 = 左「N 字 · 已保存/未保存…」+ 右「今日/连续/累计」，与预览一致；四个功能改走命令面板（`/mofei:jobs`、写作记录、**新增** 写作热力图命令）；自动保存（800ms persist + 3s saveChapter）与 Ctrl+S 保留 |
| 4 | 项目视图头部「宽幅/收起」+ 目标字数 goal 区块 | web 模式移除（功能保留在 overlay 模式；目标字数仍可经命令面板/项目数据使用） |
| 5 | 宽度差异 | 左内栏 264px → **250px**、右气泡 402px → **430px**（与预览数值一致） |
| 6 | 顶栏 | 补预览的「＋ 新建」accent 按钮（无项目=新建项目，有项目=新建章节） |
| 7 | 细节打磨 | 项目行元信息对比度提升；编辑区空态垂直居中；输入框/文本域 placeholder 统一为 tertiary 色；Agent 对话标题后缀（会话名/预设）降为小字弱色 |

## 验证结果（全绿）

```text
verify-v0.12-view.cjs       9 项（右气泡 430px）ALL PASS
verify-v0.11-chat.cjs      12 项 ALL PASS
verify-t5-linkage.cjs       4 项（.mf-proj 选择器更新）ALL PASS
verify-v0.10-workbench.cjs  7 项（.mf-proj 选择器更新）ALL PASS
verify-v0.10-isolation.cjs 21 项 ALL PASS
单元测试 11 个文件           全 PASS
verify-shots/layout-geometry.cjs  col 252px（含边框）/ editor 850px / chat 430px / 官方轨道 0
verify-shots/vision-look.cjs      vision 复核：三栏结构清晰、迷你导航底部横排确认、无布局破损
```

## 改动文件

```text
plugin/src/client/legacy.js      web 模式：迷你导航入左栏、极简项目行（.mf-proj）、
                                 行操作 hover 显现、状态栏精简、宽度 250/430、
                                 顶栏「＋ 新建」、热力图命令、placeholder/空态打磨
plugin/lib/client.js             重建 client bundle
plugin/package.json              version 0.13.1（node 写入，无 BOM）
verify-t5-linkage.cjs            .mf-grid-card → .mf-proj
verify-v0.10-workbench.cjs       .mf-grid-card → .mf-proj
verify-shots/capture-mofei.cjs   .mf-grid-card → .mf-proj
v0.13.1-changelog.md             本文档
```

## 备注

- overlay 模式（非 web 入口的侧栏面板）保持原样：宽幅/ProjectGrid/goal/状态栏按钮均保留。
- 项目行的重命名/删除在 hover 时显现（两击确认删除），与预览静止态无出入。
- 实测清单（§5）其余项：两级导航返回、会话条收放、右气泡发消息（新会话）、新建项目/章节、
  迷你导航切 tab、命令面板、专注模式 —— 全部通过。
