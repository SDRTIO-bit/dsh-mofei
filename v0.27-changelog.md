# dsh-mofei v0.27.0 变更说明（实体历史落文件树 + 回收站）

> 2026-08-21。本版把 v0.26 的「文件优先」从章节扩展到全部实体：角色 / 笔记 / 世界书的
> history/snapshot 同样从 JSON 抽离、落到 `<id>.history.jsonl`；并新增 `.mofei/trash/`
> 回收站——删除实体（含章节）时镜像与历史移入回收站而非直接删除，误删可恢复。

## 核心变更

### ① 实体历史落文件树（`.mofei-projects.json` 彻底只留索引）

| 字段 | 原位置（v0.26） | 新位置（v0.27） |
| --- | --- | --- |
| `character.history[]` | `.mofei-projects.json` | 仅 `.mofei/projects/<id>/characters/<id>.history.jsonl` |
| `note.history[]` | `.mofei-projects.json` | 仅 `.mofei/projects/<id>/notes/<id>.history.jsonl` |
| `world-entry.history[]` | `.mofei-projects.json` | 仅 `.mofei/projects/<id>/world/<id>.history.jsonl` |

- 实体 `.history.jsonl` 每条 `{ revision, at, snapshot, source? }`，JSONL 顺序追加；
  `snapshot` 为结构化快照（角色 = name/description/isFavorited；笔记 = title/content/categoryId/isLocked/isHidden；
  世界书 = name/keys/content/isEnabled/constant/order），与 `entity-history` / `rollback-entity` RPC 快照结构一致。
- `toIndexableStore()`：抽离 `entity.history`（`historySeq` 保留为元数据，驱动 revision 单调）。
- `mirrorFileTree()`：实体镜像多写一个 `<id>.history.jsonl`；`isMirrorManagedFile()` 将
  `characters|notes|world` 下的 `.history.jsonl` 纳入镜像管理（清理时按 manifest 精确处理）。
- `importFileTree()`：reload-from-files 时解析实体 `.history.jsonl` 回填 `entity.history`，
  并按「最后已用 revision」恢复 `historySeq`——只升不降，回填后继续更新 revision 依然单调。
- 删除链路：删角色/笔记/世界书时其 `.history.jsonl` 一并移入回收站（见 ②）。

### ② 回收站（`.mofei/trash/`）

- 删除章节 / 角色 / 笔记 / 世界书时，镜像 `.md` 与其 `.history.jsonl` **不再直接删除**，
  而是移入 `工作区/.mofei/trash/<projectId>/<时间戳>-<实体id>/<原相对路径>`；
  同一实体的 `.md` 与 `.history.jsonl` 共享同一批次目录，便于日后整体恢复。
- 回收站位于 `projects` 树之外：`reload-from-files` 不会把它扫回 store，`fileTreeStamp`
  外部变更检测也不受影响。
- 跨盘 rename 失败时回退为「复制 + 删除」，尽力保住回收站语义。
- 新增只读 RPC `trash-list`：按项目 / 批次列出回收站文件（含时间戳 `at`），
  供日后 UI / 手工（或 git）恢复；本版不实现自动 restore/purge。
- **删除顺序修正**（关键）：所有 delete RPC（project / chapter / volume / character / note /
  world-entry / world-entries / prompt-chain）改为「先移回收站，再 `saveProjects()`」——
  `mirrorFileTree` 会按 manifest 清理已不在 store 的镜像文件，顺序反了回收站就接不到文件
  （v0.26 的「删除即消失」语义仍成立，只是文件去了回收站）。

## 升级不变量

1. **不破坏旧数据**：旧 `.mofei-projects.json`（带 `entity.history`）依旧能 load；
   下一次 `saveProjects()` 自动抽离实体历史到文件树。
2. **entity-history / rollback-entity 不变**：读 in-memory `entity.history`，save 时落盘
   `.history.jsonl`，reload 后再读回；快照结构、`source` 标记、`ENTITY_HISTORY_MAX` 上限照旧。
3. **revision 单调**：`historySeq` 语义 = 「最后已用 revision」，回填只补不低于文件树
   最后一条 revision 的值，绝不抬升（避免 push 时跳号）。
4. **并发安全 / git / 编辑器上限** 等其余不变量与 v0.26 完全一致。

## 兼容性矩阵

| 客户端能力 | 旧 JSON（含 entity.history） | 新 JSON（无 entity.history） |
| --- | --- | --- |
| `entity-history` | 返回内存 history | 同左（内存 history 来自 `.history.jsonl` 回填） |
| `rollback-entity` | 按 revision 回退 | 同左 |
| 直接读 `.mofei-projects.json` | 能看到实体历史 | 只能看到索引（历史读 `characters|notes|world/<id>.history.jsonl`） |
| 删除实体后 | 镜像消失 | 镜像移入 `.mofei/trash/<projectId>/`（可恢复） |

## 验证

```powershell
node --check plugin/lib/index.js
node test-host.mjs                       # 55/55（v0.27 两项新测试 + 既有 53 项）
node test-roles.mjs                      # ROLE CATALOG PASS
node test-role-host.mjs                  # ROLE HOST PASS
node test-mofei-writer-preset.mjs        # MOFEI WRITER PRESET CONTRACT PASS
node test-subagent-max-role.mjs          # SUBAGENT ROLE RESOLUTION PASS
node test-txt.mjs                        # 9/9
node test-world.mjs                      # 7/7
node test-ai.mjs                         # 7/7
node test-summary.mjs                    # 10/10
node test-prompt-chain.mjs               # 15/15
```

v0.27 新增/改造测试（`test-host.mjs`）：

- `v0.27 实体历史：JSON 抽离 + <id>.history.jsonl 落盘 + 第二实例回填 + revision 单调`：
  角色（update + rollback 两条快照）/ 笔记（两次 update 快照变更前状态）/ 世界书各写历史，
  断言 JSON 无 `entity.history`、三种 `.history.jsonl` 内容与快照正确、第二实例回填后
  `entity-history` 完整、继续更新 revision 严格递增（3 > 2）。
- `v0.27 回收站：删除章节/角色/笔记/世界书 → 镜像与历史移入 .mofei/trash 而非删除；reload 不复活`：
  四类删除后原位置 `.md` + `.history.jsonl` 均为 ENOENT、回收站按批次找回全部 8 个文件、
  `trash-list` 分组可见且带 `at`、`reload-from-files` 不复活、回收站文件不受 reload 影响。
- 原 `实体历史持久化`（virtual-root 内存 fs）重写为真实文件树双实例测试——v0.26 起实体历史
  已不在 JSON，持久化验证必须走文件树回填路径。

## 待办（v0.28+ 候选）

- `[x]` 实体历史（角色 / 笔记 / 世界书）落文件树 → v0.27
- `[x]` 删除章节 `.history.jsonl` 进回收站 → v0.27（已扩展为全部实体 + `trash-list` RPC）
- `[ ]` 回收站 UI：Studio 回收站视图（列出 / 恢复 / 清空）—— v0.28 候选
- `[ ]` RAG 索引重建：file-first 后索引构建可省略 chapter.content 拉取，直接读 `.md`—— v0.28 候选
