# mofei-dsh v0.10.0 变更说明（写作内容平台重构）

> 生成：2026-08-16。基线 v0.9.1。
> 定义：墨扉 = MOFEI-SPEC（文件规范）+ MOFEI-AGENT（写作 preset/技能/工具）+ MOFEI-STUDIO（长期写作工作台）。

## 新增 / 变更

### 1. 区间隔离（Agent Preset）

- Host core 不再注册任何 `mofei_*` 工具或 `mofei-*` 技能。
- 新增子入口插件：
  - `mofei-dsh/tools` → 33 个 mofei_* + 33 个 openfic_* 工具
  - `mofei-dsh/skills` → 17 个 mofei-* + 17 个 openfic-* 技能
- 新增写作 preset：`~/.dsh/.agent-presets/mofei-writer/`
- 实测：standard 会话 `{mofei:0, openfic:0}`；mofei-writer 会话 `{mofei:33, openfic:33}`。

### 2. MOFEI-SPEC 文件优先镜像

- 运行时自动镜像 `.mofei/projects/<id>/**`：
  - `project.yml`、`chapters/**/*.md`、`characters/*.md`、`notes/*.md`
  - `world/*.md`、`summaries/**/*.md`、`chains/*.md`
- 章节/角色/世界/笔记正文都是 Markdown + YAML frontmatter。
- 删除项目同步清理镜像目录。
- 规范文档：`docs/MOFEI-SPEC.md`。

### 3. 写作提示词入口（风格）

- `styles/*.md`：default / plain（白描）/ classical（古风）/ light-novel（轻小说）。
- RPC：`list-styles` / `get-style` / `set-project-style` / `get-project-style`。
- `project.yml.currentStyle` 记录当前风格。
- `chapter-context` 自动注入当前风格提示词。
- Studio 标题栏提供风格选择器。

### 4. 工具极简化

- 新增通用三件套：`mofei_read` / `mofei_write` / `mofei_edit`。
- 实体别名：`read/write/edit-chapter|character|note|world-entry`。
- 特殊领域工具：`mofei_list` / `mofei_search` / `mofei_context` /
  `mofei_summarize` / `mofei_retrieve`（预留）/ `mofei_history` / `mofei_revert`。
- `edit-chapter` 为真实行区间替换 + revision 保护。
- 保留 v0.9 旧名兼容工具。

### 5. MOFEI-STUDIO

- 普通 DSH Web 只保留一个 `sidebar.footer.action` 入口；移除浮动按钮和 cordis 工具卡片。
- 点击入口后才注入 CSS 并打开**全屏工作台**；退出即移除 CSS/DOM。
- 命令面板：`Ctrl+Shift+P`，支持新建项目/章节、摘要、链、写作记录、退出。
- 标题栏：项目名、当前风格、导入/导出、命令、退出。
- 状态栏：字数、revision、目标、今日/连续/累计、写作记录/热力图/保存。
- `/mofei/` 独立站点保留。

## 验证结果

```text
test-host                      46/46
verify-agent-isolation         standard 0/0 + mofei-writer 32+32 ALL PASS
verify-agent-tools             mofei-writer 66 tools ALL PASS
verify-agent-surface           skills 17+17 ALL PASS
verify-subagent-pipeline       Writer done/revision 3 + Reviewer PASS ALL PASS
verify-spec                    文件镜像 + 风格注入 ALL PASS
verify-v0.10-isolation         入口唯一 + 全屏 + 退出无残留 ALL PASS
verify-v0.10-workbench         三栏/标签/状态栏/风格/命令面板 ALL PASS
verify-v0.6/v0.7/v0.8/v0.9/v7/v5-ui 六项浏览器回归 ALL PASS
verify-writing-pipeline        ALL PASS
```

## 改动文件

```text
plugin/lib/index.js             core 服务化 + 文件镜像 + 风格 RPC + edit-chapter
plugin/lib/tools.js             新增：Agent 工具插件
plugin/lib/skills-plugin.js     新增：写作技能插件
plugin/package.json             version 0.10.0 + exports ./tools ./skills
plugin/src/client/legacy.js     全屏 Studio + 命令面板 + 风格选择器 + 惰性 CSS
test-host.mjs                   工具/技能插件分层测试
tools/verify-agent-isolation.mjs  新增
tools/verify-agent-surface.mjs    更新 mofei-writer preset
tools/verify-agent-tools.mjs      更新 64 工具预期
tools/verify-subagent-pipeline.mjs 更新 mofei-writer preset
tools/verify-spec.mjs           新增
verify-v0.10-isolation.cjs      新增
verify-v0.10-workbench.cjs      新增
docs/MOFEI-SPEC.md              新增
styles/*.md                     新增 4 个写作风格
~/.dsh/.agent-presets/mofei-writer/*  新增写作 preset
PLAN-v0.10-workbench.md         计划
```
