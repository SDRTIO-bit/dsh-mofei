# mofei-dsh v0.18.0 变更说明（初始向导/小说文件夹 + 会话入口 + 质感统一）

> 生成：2026-08-16。基线 v0.17.1（commit `0248f23`）。
> 需求来源（用户）：
> 1. 项目开始的**准备阶段**：空白状态引导选择「小说文件夹」，之后一切文件保存在那里；
> 2. Bug A：打开墨菲后无法直接选择对话历史（要退出墨菲去官方 DSH 选会话再进来）；
> 3. Bug B：墨菲界面质感与原版 DSH 有差异。
> 计划与方案：`PLAN-v0.18-onboard-acceptance.md`。

## 功能

### 初始向导（准备阶段）

- **触发**：进入墨菲且没有任何项目 → 全屏向导「开始你的第一本小说」；
- 流程：点「选择文件夹」→ 调用 DSH 原生 `host.pickDirectory`（Windows 目录对话框）→ 显示所选路径 →
  输入小说名 → 「开始写作」；
- **项目级 rootDir（小说文件夹）**：创建后该小说的全部实体文件
  （章节/角色/世界书/笔记/链/摘要/project.yml）镜像到所选文件夹，工作区 `.mofei/projects/<id>`
  不再产生实体镜像；文件优先读回、`file-tree-status`、变更轮询（`sync-status`）均按 rootDir 工作；
- `delete-project` **不删除**小说文件夹（用户数据安全）；
- 不选文件夹直接开始 = 沿用默认位置（兼容老项目，零迁移）。

### Bug A：会话入口

- 顶栏按钮「写作助手」→「**会话**」（title：选择/切换历史会话或新建写作会话）；
- **自动弹出**：打开墨菲且未绑定会话、存在历史会话时，会话菜单自动展开——直接点击历史会话承接上次对话，
  无需退出墨菲；
- 修复过程中定位两个真问题：
  1. effect 依赖整份 `chatSessionList` 快照（276 个会话频繁更新）→ 每次更新清掉 `later` 定时器，永远弹不出；
     → 改 ref 读取，依赖收敛为「列表是否非空」信号；
  2. 官方会话快照冷启动为空、数据异步到达 → 依赖加入非空信号后正确触发。

### Bug B：质感统一

- 墨菲工作台/气泡/菜单的硬编码色（`#0d0e11`/`#101115`/`#111217`/`#141416`、半透明边框）全部替换为
  `--dsw-alias-*` 令牌——与官方 DSH 组件共享同一套墨韵色板，并随主题切换联动。

### 回归脚本适配

- `verify-v0.14-writing.cjs` / `verify-v0.17-skills-sessions.cjs`：
  菜单交互改幂等（自动弹出后不再重复点击切换）；「会话」按钮断言适配新结构；
  v0.14.1 时代「菜单不列全局会话」的过时断言更新为「列出全部会话」语义。

## 验证结果（全绿）

```text
verify-v0.18-onboard.cjs      10 项 ALL PASS：
  rootDir 创建/落盘/文件优先读回/file-tree-status/删除不删目录 + 会话入口/自动弹出/全部会话
verify-v0.14-view/chat/writing/isolation/workbench   回归 ALL PASS
verify-v0.15-sync / v0.16-subagent / v0.17-skills-sessions   回归 ALL PASS
单元测试 12 个文件  全 PASS
vision-look 截图审查：质感统一无异常（无硬编码色残留、无重叠/对比度问题）
```

## 改动文件

```text
plugin/lib/index.js           projectFileBase + mirror/import/file-tree-status/fileStamp 支持 rootDir；
                              create-project 接受 rootDir；get-project-root RPC；导入时继承 rootDir
plugin/src/client/legacy.js   初始向导（pickDirectory/表单/创建）；「会话」按钮 + 自动弹出（ref+非空信号）；
                              硬编码色 → 令牌
plugin/lib/client.js          重建 bundle
verify-v0.14-writing.cjs      断言适配（菜单幂等/按钮结构）
verify-v0.17-skills-sessions.cjs  菜单交互幂等
verify-v0.18-onboard.cjs      新增端到端回归
PLAN-v0.18-onboard-acceptance.md  计划文档
v0.18-changelog.md            本文档
docs/ACCEPTANCE-2026-08.md    整体验收报告
```

## 备注

- 初始向导 UI 需在**空 store**（首次使用/删除全部项目后）出现；当前环境已有项目，故向导由
  `verify-v0.18-onboard.cjs` 以 RPC 链路覆盖（rootDir 全流程），UI 呈现待用户首次使用/清空后确认；
- 向导「选择文件夹」弹出的是 Windows 原生目录对话框（需在 3088 所在机器上操作）。
