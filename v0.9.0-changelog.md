# mofei-dsh v0.9.0 变更说明（写作记录仪表盘 / prompt chains 简版 / 继续模块化）

> 生成：2026-08-15。基线：v0.8.0 全绿。子代理 A/B/C 并行交付 + 主代理集成。

## 新增

1. **写作记录仪表盘（工作包 B + 主集成）**
   - 页脚「写作记录」按钮打开面板：日期范围（起始/截止 date 输入）+ 快捷「近7天 / 近30天 / 全部」。
   - 统计条：N 天 · 共 M 字 · 日均 A 字；明细按日期升序：日期 / 周几 / N 字。
   - 数据源：`stats.calendar`（按日字数），组件纯展示不 fetch。
   - `writing-dashboard.js`：dailyRows / defaultRange / rangeStats / weekdayName（34 项单测）。

2. **prompt chains 简版（工作包 A/C + 主集成）**
   - Host 持久化 `.mofei-chains.json`（按项目分组）；`delete-project` 自动清理。
   - RPC：`list-prompt-chains` / `save-prompt-chain`（upsert） / `delete-prompt-chain` /
     `compile-prompt-chain` / `run-prompt-chain`（LLM 生成并写入 AI 会话历史）。
   - 宏编译：`{{project}} {{chapter}} {{chapterText}} {{selected}} {{characters}} {{world}} {{notes}} {{instruction}}`，
     上下文自动组装（章节前 1.2 万字、角色/世界书/笔记各前 200 字、启用世界书、未隐藏笔记）。
   - UI：编辑器头部「链」按钮 → 左链列表 / 右名称+内容编辑器 / 保存（新建）/ 运行 / 删除（两击确认）/
     结果与编译提示词展示。
   - 旧 Host 上打开显示「需重启 DSH 后可用」；重启后可用。
   - `prompt-chain.js`（15 测）、`prompt-chains.js`（35 测）、test-host **46/46**（新增 7 项）。

3. **legacy.js 继续模块化**
   - 抽离 `workspace-utils.js`：`fmtTime / dateKey / countWords`（3 测），legacy.js 改 import。
   - 本批又新增 3 个独立 client 模块（writing-dashboard / prompt-chains / workspace-utils），
     legacy.js 仅负责组装与数据接线（后续仍可继续按功能拆分）。

## 改动文件

```text
plugin/lib/prompt-chain.js           新增：宏编译/链视图纯逻辑
test-prompt-chain.mjs                新增：15 项
plugin/lib/index.js                  .mofei-chains.json 持久化 + 5 个 RPC + delete-project 清理
plugin/src/client/writing-dashboard.js 新增：写作记录仪表盘组件
plugin/src/client/writing-dashboard.test.mjs 新增：34 项
plugin/src/client/prompt-chains.js   新增：prompt chains 面板组件
plugin/src/client/prompt-chains.test.mjs 新增：35 项
plugin/src/client/workspace-utils.js 新增：fmtTime/dateKey/countWords
plugin/src/client/workspace-utils.test.mjs 新增：3 项
plugin/src/client/legacy.js          集成仪表盘/链面板 + import workspace-utils
plugin/lib/client.js                 esbuild 重新打包产物
test-host.mjs                        46 项（新增 prompt chains 集成 7 项）
verify-v0.9.cjs                      新增：v0.9 浏览器验收
plugin/package.json                  version 0.9.0
```

## 验证结果（全绿）

```text
自动化：txt 9 · world 7 · ai 7 · summary 10 · prompt-chain 15 · grid 22 · page 23 ·
        layout 19 · limits 16 · summary-panel 40 · worldbook 21 · agent-bridge 26 ·
        dashboard 34 · chains 35 · utils 3 · test-host 46 + pipeline smoke ALL PASS
浏览器：verify-v0.6 / v0.7 / v0.8 / v7 / v5-ui / v0.9 全部 ALL PASS
        （v0.9 prompt chains 在旧 Host 上按预期 SKIP，重启后走真实链列表）
```

## 待重启清单（累计，重启后一次生效）

- v0.5：5 个 Agent 工具 output schema 修复。
- v0.7：chapter-summaries / 摘要 SSE / rangeIds / force / 世界书批量。
- v0.8：entity-history / rollback-entity。
- v0.9：prompt chains 五个 RPC。
- 重启后复跑：verify-v0.7 / verify-v0.8 / verify-v0.9；真实子代理 Writer→Reviewer 流水线实测。
