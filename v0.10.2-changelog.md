# mofei-dsh v0.10.2 变更说明（Git 适配器 + OpenFic 借鉴改进 + DSH Jobs 长任务）

> 生成：2026-08。基线 v0.10.1。
> 本轮按 NEXT-SESSION §2 执行 A（Git 适配器补全）+ B（OpenFic 小改进）+ C（长任务接 DSH Jobs）。

## 新增 / 变更

### A. Git 适配器补全（P3）

- **全实体写操作自动 git 提交**：`saveProjects` 钩入 `gitCommitAll`（章节/角色/笔记/世界书/项目/风格等全部写操作自动提交 `.mofei`）。
  - 节流：每 10s 至多一次 commit（尾随合并），自动保存不会刷屏历史。
  - **串行队列 + 保存等待提交完成**：修复「git add 进行中写入被并入前一个提交」竞态（此前链保存会丢失独立提交）。
  - 中文 commit message 经 UTF-8 临时文件 + `git commit -F` 写入（Windows 下 `-m` 会损坏中文）。
- 链保存/删除**立即提交**（离散操作不受节流）。
- 新增 RPC：`git-diff`（项目/链任意两提交 unified diff，默认 HEAD~1..HEAD）、`git-revert-project`（文件树回滚到指定提交 + preferFiles 强制导入 + revision 回落）。
- `importFileTree({ preferFiles: true })`：显式回滚时文件无条件胜出（revision 冲突保护让位于用户意图）。
- 新增 Agent 工具（mofei_* + openfic_* 别名，共 36+36）：
  - `mofei_project-history`（项目/链 git 提交史 + diff）
  - `mofei_diff-revision`（任意两提交 diff）
  - `mofei_revert-project`（回滚到指定提交）
- Studio git 面板增强：每提交「回滚」按钮（arm 确认）、「含 diff」开关、刷新。
- 非 git 工作区全部优雅降级（`available:false` + 原因）。

### B. OpenFic 借鉴小改进（P2）

- **分级上下文注入**（`world.js buildChapterContext` 第 4 参 summaries）：
  - mid = 前情章节摘要（`前情摘要（mid）`）
  - far = 覆盖前情的区间摘要（`区间摘要（far）`）
  - `chapter-context` RPC 注入 summaryStore 数据源；`buildAiAssistRequest` 等其他调用点不传则行为不变。
- **检索结果卷字段**：`retrieve` 章节/摘要命中携带 `volumeId / volumeTitle / chapterId`；Studio 检索视图按卷/实体类型分组展示。
- 调研结论确认：摘要签名链兜底「无需改动」（外部编辑不 bump revision 时 import 不采纳，revision 门控已是最简契约，见 docs/OPENFIC-RESEARCH.md）。

### C. 长任务接 DSH Jobs（P3）

- 挂接 DSH jobs controller（`attachController`），无 jobs 服务时忽略。
- 新增 RPC：`job-start-summarize`（章节/区间摘要后台任务，复用 LLM 桥接与摘要持久化）、`job-list-mofei`（进度/状态/当前项）、`job-kill-mofei`（取消，经 DSH jobs kill + 本地 cancel 协作）、`job-result-mofei`（完成回填 summaries）。
- Studio：状态栏「任务」按钮 + 后台任务面板（2s 轮询，进度 done/total、当前处理章节、取消按钮）；AI 面板「批量摘要」改走 Jobs（`JOBS_UNAVAILABLE` 时降级原同步路径）；命令面板 `/mofei:jobs`。
- 取消路径实测：kill 后任务终止（failed/JOB_CANCELED）。

## 验证结果

```text
test-host                          46/46（工具 36+36）
verify-spec                        文件树回读/冲突/status ALL PASS
verify-retrieve-style              结构化命中 + 百万字冷 148ms/热 1ms + 风格注入 + 分级上下文 + 卷字段 ALL PASS
verify-git-history                 链立即提交 ×2 + 中文信息 + git-diff + 项目历史 + 回滚（正文/revision 回落）ALL PASS
verify-jobs                        启动/进度/完成/幂等（fresh 0 条）/取消 ALL PASS
verify-agent-isolation/tools/surface  ALL PASS（36+36/72）
verify-subagent-pipeline           ALL PASS
agent-bridge.test                 29/29
verify-v0.10-workbench/isolation + 旧六项浏览器回归  ALL PASS
```

## 改动文件

```text
plugin/lib/index.js                全实体自动提交（节流+串行）+ git-diff/revert RPC + preferFiles 导入
                                   + jobs 集成（start/list/kill/result）+ 分级上下文注入 + 检索卷字段
plugin/lib/world.js                buildChapterContext 第 4 参（mid/far 摘要注入）
plugin/lib/tools.js                +3 工具（project-history / diff-revision / revert-project）
plugin/lib/client.js               重建 client bundle
plugin/src/client/legacy.js        任务面板 + git 面板（回滚/含diff）+ 检索分组 + 批量摘要走 Jobs
plugin/package.json                version 0.10.2
tools/verify-git-history.mjs       扩展：diff + 回滚 + 跨窗口节流
tools/verify-jobs.mjs              新增：Jobs 验收
tools/verify-retrieve-style.mjs    扩展：分级上下文 + 卷字段
test-host.mjs / verify-agent-*.mjs 工具数 33→36、总数 66→72
```

## 备注

- 工作区 git 仓库继续只跟踪 `.mofei/`；自动提交节流 10s（`GIT_COMMIT_INTERVAL_MS`），链操作即时。
- 已知边界：节流窗口内的多次写会合并为一次提交（历史粒度 10s）；回滚会重置章节 revision 到历史值（符合语义）。
- P3 剩余：导出/索引等其他长任务接入 Jobs、`mofei_project-history` 之外的工具审计接入实体 history（见 NEXT-SESSION §2）。
