# mofei-dsh v0.3.0 变更说明（待重启验证）

> 基线：v0.2.0（TXT 导入导出 / 查找替换 / 多标签 / AI 助手 / 8 个 Agent 工具）。
> 数据兼容：沿用 `.mofei-projects.json` version 4；每个 project 新增可选字段 `worldEntries`，旧数据 load 时自动补 `[]`。

## 新增

1. **TXT 编码检测（Client）**
   - 顺序：UTF-8/UTF-16 BOM → UTF-8 严格解码 → GB18030 → Big5 → UTF-8 替换解码。
   - 导入弹窗显示“检测编码：…”。
2. **世界书 world-info**
   - 数据模型：`worldEntry = { id, name, keys[], content, isEnabled, constant, order }`。
   - RPC：`create/update/delete/move-world-entry`、`import-world-info-json`。
   - SillyTavern Lorebook JSON 导入：`entries.keys / secondary_keys / comment / content / constant / selective / disable / order`，支持 append / overwrite。
   - 激活规则：禁用不注入；`constant` 常驻；`keys` 命中章节正文时激活；无 keys 时条目名命中激活。
   - Client 左侧新增「世界」页签：新建 / 排序 / 常驻 / 启用开关 / 删除 / ST JSON 导入。
3. **chapter-context RPC**
   - 返回：项目信息、章节尾、角色、未隐藏笔记、当前章节激活的世界书、前情章节、`contextText`。
   - AI 助手自动改用该上下文（世界书 + 前情进入 system prompt）。
4. **Agent 工具 8 → 13 个**
   - 新增：`mofei_list-world-entries` / `mofei_get-chapter-context` / `mofei_create-world-entry` / `mofei_update-world-entry` / `mofei_delete-world-entry`。

## 改动文件

```text
plugin/lib/index.js    世界书 CRUD + chapter-context + AI 上下文升级 + 13 Agent 工具 + ai-assist content-block/finish-error 修复
plugin/lib/world.js    新增：世界书/上下文纯逻辑
plugin/lib/client.js   世界书页签 + ST JSON 导入弹窗 + TXT 编码检测
plugin/package.json    v0.3.0 + files 含 lib/world.js
test-world.mjs         新增：7 项纯逻辑单测
test-host.mjs          新增：10 项 Host RPC 集成冒烟（含工具名正则 + AI 流 mock）
```

## 验证清单（2026-08-15 已执行）

- [x] `test-txt.mjs` 9/9、`test-world.mjs` 7/7、`test-host.mjs` 10/10（含工具名正则 + AI 流 mock）
- [x] `verify-v6.cjs` ALL PASS（GB18030 / 世界书 / ST JSON / chapter-context）
- [x] `verify-v5.cjs`（v0.2 回归）ALL PASS、`verify-fixed.cjs`（v0.1 基线）ALL PASS
- [x] 左侧「世界」页签：新建条目 → 保存 → 常驻/禁用 → 删除（两击确认）
- [x] 世界书页签「导入」：ST Lorebook JSON → 显示导入条数
- [x] 导入 GB18030 TXT：无乱码且显示检测编码
- [ ] `Tool.listTools` 手动确认 13 个 `mofei_*`（会话内）

## 回退

- 世界书仅新增可选字段与 RPC，不影响旧协议；若异常，注释 `index.js` 中 v6 世界书 handlers 与新增工具定义即可回到 v0.2.0。
- 动态版应急源码仍在 `source\pkg-22-createchapter-fix.*`。
