# dsh-mofei v0.5.0 变更说明（平台体验批，已重启验证通过；含验证修复）

> 基线：v0.5.0 品牌版（墨扉/Mofei + SSE 流式 + 旧名兼容，18 个 mofei_* 工具 / 17 skills）。
> 本轮范围：docs/archive/SUBAGENT-HANDOFF.md 工作包 A/B/C，并行完成，主代理统一集成。
> 数据兼容：新增 `.mofei-summaries.json`（version 1）；其余数据文件结构不变。

## 新增

1. **编辑器体验批（工作包 A，`plugin/src/client/legacy.js`）**
   - 正文自动保存：180 秒 → 3 秒（800ms 草稿 debounce、冲突/status/saving 逻辑原样保留）。
   - 编辑器内联章节标题 input（`mf-title-input`）：Enter/blur 提交 `update-chapter-meta`，成功同步项目章节与标签页标题，失败只 setError 不覆盖正文。
   - 字数统计改为中文口径 `countWords(text) = String(text).replace(/\s+/g, '').length`，页脚显示「N 字」。
   - Tab 键插入两个全角空格 `\u3000\u3000`（无 Ctrl/Alt/Meta），光标移到插入内容之后。
   - 上次章节恢复：`localStorage['mofei.lastChapter.<projectId>']`，项目加载且未选章节时自动恢复；删除章节时清理对应 key。

2. **项目网格页（工作包 C，`plugin/src/client/project-grid.js`）**
   - 网格/列表视图切换（默认网格）、标题/简介大小写不敏感模糊搜索、排序（updated/created 降序、title 按 `localeCompare('zh-Hans-CN')`）。
   - 卡片：首字/emoji 封面、章节数、目标进度（有正文按 content 长度，缺正文按 order 估算，全无正文显示「—」）、重命名/删除（删除两击确认 + 3s 超时）。
   - 纯函数 `filterProjects` / `sortProjects` 导出；`PROJECT_GRID_CSS` + `ensureGridStyles()`（`data-mf-grid` 去重注入）；组件不调用 fetch/RPC。
   - 已接入 legacy.js 项目页 tab，替换原列表；React 经 `globalThis.__mofeiReact` 注入点解析。

3. **摘要体系（工作包 B，`plugin/lib/summary.js` + Host 集成）**
   - 纯逻辑：`normalizeSummaryStore` / `chapterSummaryView` / `isChapterSummaryStale` / `applyChapterSummary` / `buildRangeGroups` / `applyRangeSummary` / `planSummaryBatch`（全部不可变写入、脏数据安全）。
   - Host 持久化：`.mofei-summaries.json`（章节摘要 + 区间摘要），删除项目/章节/卷时自动清理对应摘要条目。
   - 批量摘要过期重算：`ai-summarize-chapters` 只对缺失/修订变化/超期（默认 30 天）的章节调 LLM，结果落盘；新增区间摘要批量生成 `ai-summarize-ranges`（每 10 章一组，同样只算过期区间）。
   - 新增 RPC：`chapter-summary` / `save-chapter-summary` / `summary-plan` / `range-summary-groups` / `save-range-summary` / `ai-summarize-ranges`。

4. **Agent 工具 18 → 23 个**
   - 新增 `mofei_get-chapter-summary` / `mofei_save-chapter-summary` / `mofei_get-range-summaries` / `mofei_save-range-summary` / `mofei_summarize-ranges`。
   - `mofei_summarize-chapters` 描述与参数更新（`maxAgeDays`）。
   - 连同 `openfic_*` 旧名 alias 共 46 个注册。

## 改动文件

```text
plugin/src/client/legacy.js          工作包 A 五项 + 接入 ProjectGrid
plugin/src/client/project-grid.js    新增：项目网格组件（纯函数 + 样式注入）
plugin/src/client/project-grid.test.mjs 新增：filter/sort 单测 22 项
plugin/lib/summary.js                新增：摘要体系纯逻辑（7 个导出）
plugin/lib/index.js                  摘要持久化 + 6 个 RPC + 批量过期重算 + 清理 + 23 工具
plugin/lib/client.js                 esbuild 重新打包产物
test-summary.mjs                     新增：纯逻辑单测 10 项（含覆盖写入回归）
test-host.mjs                        23 项（新增摘要 RPC 集成测试 5 项 + 工具输出契约 1 项）
tools/verify-client-bundle.mjs       新增：Workspace 未声明 setter 静态检查
verify-v5-ui.cjs                     新增：v0.5.0 新功能浏览器验收
README.md / HANDOFF.md               状态与测试清单更新
```

## 验证结果（node 级，全部通过）

```text
node --check plugin\lib\index.js / summary.js / src\client\legacy.js / project-grid.js  OK
node plugin\esbuild.config.js                                                        Build complete!
node tools\verify-client-bundle.mjs                                                  CLIENT BUNDLE CONTRACT OK
node test-txt.mjs           9/9
node test-world.mjs         7/7
node test-ai.mjs            7/7
node test-summary.mjs       10/10
node plugin\src\client\project-grid.test.mjs   22/22
node test-host.mjs          23/23（新增「工具 output schema 与真实返回形状一致」契约测试）
node verify-v7.cjs          ALL PASS（浏览器回归）
node verify-v5-ui.cjs       ALL PASS（v0.5.0 新功能浏览器验收，新增）
```

## 重启验收修复（2026-08-15）

首次浏览器验证发现并修复以下问题（修复均带回归测试或浏览器验收覆盖）：

1. **编辑器打开章节即崩溃**：`saved/setSaved` 引用但未声明，React 渲染抛
   `ReferenceError` 导致工作区卸载。已补 `React.useState('')`；
   `tools/verify-client-bundle.mjs` 新增「Workspace 未声明 setter」静态检查防回归。
2. **刷新后不能自动恢复上次章节**：只持久化了 `lastChapter.<projectId>`，
   没有持久化最后项目 id。新增 `localStorage['mofei.lastProject']`，
   在 `pickProject`/`createProject` 写入、bootstrap 加载后自动恢复；删除项目时一并清理。
3. **项目网格两击删除实际需要四击**：ProjectGrid 自身已做两击确认，
   但 `onDelete` 又进入 legacy `deleteProject` 的二次确认。改为
   `deleteProject(id, confirmed)` 跳过 legacy 确认（保留旧入口兼容）。
4. **5 个 Agent 工具 output schema 与真实返回错配**（DSH 严格校验 `additionalProperties:false`）：
   - `mofei_read-chapter`：补 `order/historyCount`，`volumeId` 允许 null。
   - `mofei_update-chapter`：补 `stats`、`conflict`、`expectedRevision`、`actualRevision`。
   - `mofei_summarize-chapters`：补 `fresh`。
   - `mofei_get-chapter-summary`：`entry` 允许 null。
   - `mofei_summarize-ranges`：补 `staleCount`、`freshCount`。
   `test-host.mjs` 新增契约测试，实际执行 23 个 `mofei_*` 工具并校验输出形状。
5. 清理了 `deleteProject` 未清 `mofei.lastChapter.<projectId>` 的已知小问题（顺带修复）。

## 验证清单（重启后）

- [x] `Tool.listTools` 可见 23 个 `mofei_*`（+23 个 openfic_* alias；源计数 23×2=46）。
- [x] `verify-v7.cjs` ALL PASS（Markdown 工具条/热力图/拖拽/AI 面板）。
- [x] `verify-v5-ui.cjs` ALL PASS：网格/列表切换、搜索、排序、重命名、两击删除 + 3s 超时。
- [x] 编辑器内联标题改后 Enter → 标签页与侧栏标题同步；失败路径不覆盖正文。
- [x] Tab 插入两个全角空格；页脚「N 字」；正文 3 秒自动保存。
- [x] 刷新页面后自动恢复上次打开的章节。
- [x] 批量摘要第二次运行只重算过期章节（实测 `staleCount=0/freshCount=2`，修订后 `staleCount=1/freshCount=1`）。
- [x] 区间摘要：`ai-summarize-ranges` 生成后 `range-summary-groups` 可见（title `第1-N章`）。

## 回退

- 项目网格：删除 legacy.js 中的 `ProjectGrid` import 与渲染段，恢复原 `projects.map(...)` 列表即可。
- 摘要体系：注释 `summary.js` import、新增 handlers 与 5 个工具定义即可；`.mofei-summaries.json` 可整体删除（自动重建空 store）。
