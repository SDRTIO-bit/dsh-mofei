# dsh-mofei v0.28.0 变更说明（RAG 索引正文直读文件树）

> 2026-08-21。v0.26/v0.27 落地「文件优先」后，`.md` 文件树成为正文的唯一来源。
> 本版让 RAG 索引构建也遵循这一事实：`buildIndex` 支持异步 `readContent` 加载器，
> 索引正文直接读 `.mofei/projects/<id>/**`（或 rootDir 项目目录）下的 `.md`，
> 不再依赖内存 store 的 content 字段。

## 核心变更

### `plugin/lib/rag.js`

- `buildIndex(project, summaries, config, options)` 升级为 **async**，新增可选
  `options.readContent(kind, entityId) => Promise<string|null>`：
  - 提供加载器：正文以文件树为准（加载器优先，返回 null / 抛错时回退内存字段）；
  - 不提供：行为与旧版完全一致（内存字段），旧调用方只需补 `await`。
- `sourceSignature(project, summaries, contentOf)`：签名与 chunks **共用同一份加载结果**
  （内部 contentCache），保证 `indexStatus` 的 fresh/stale 判定与索引内容一致——
  加载器读到的正文变了，签名就变，索引自动判 stale。

### `plugin/lib/index.js`

- 新增 `fileTreeBodyLoader(project)`：一次扫描 `chapters|characters|notes|world` 目录，
  按 frontmatter `id` 建立 `kind:id → body` 映射，返回加载器闭包；
  virtual-root（测试）环境下自然回退空映射 → buildIndex 走内存字段。
- `rag-build-index` RPC 与 `buildRagIndex` service 均改为：
  `await buildIndex(project, summaries, config, { readContent: await fileTreeBodyLoader(project) })`。

### 意义

- 索引正文来源与 v0.26+ 的「文件树是唯一正文来源」哲学对齐；将来若 load 从
  「每次全量 importFileTree」优化为按需回填，RAG 索引依然正确。
- 加载器直读磁盘，不把全部正文复制进索引构建路径以外的内存结构。
- 摘要（summary）chunk 不经过加载器，仍走内存（摘要本身以 JSON 为持久层）。

## 升级不变量

1. **旧调用兼容**：`buildIndex` 由同步变 async，属破坏性签名变更，但全部调用方
   （`rag-build-index` RPC、`buildRagIndex` service）已同步更新；外部直接 import
   `rag.js` 的用法需补 `await`（本项目内已无同步调用点）。
2. **检索/状态语义不变**：`queryIndex`、`indexStatus`、RRF 混合、rerank 均未改动。
3. **virtual-root 测试环境**：loader 读到空映射 → 回退内存字段，既有测试零变化。

## 验证

```powershell
node --check plugin/lib/rag.js
node --check plugin/lib/index.js
node test-host.mjs                       # 56/56（v0.28 新增 1 项 + 既有 55 项）
node test-rag.mjs                        # 7/7（v0.28 新增 RAG 纯逻辑单测）
node test-roles.mjs / test-role-host.mjs / test-mofei-writer-preset.mjs
node test-subagent-max-role.mjs / test-txt.mjs / test-world.mjs
node test-ai.mjs / test-summary.mjs / test-prompt-chain.mjs   # 全 PASS
```

v0.28 新增测试：

- `test-rag.mjs`（7 项纯逻辑）：无加载器 = 旧行为；加载器优先覆盖内存；加载器返回
  null / 抛错回退内存；签名随加载器内容变化；`queryIndex` 对加载器构建的索引正常检索；
  `chunkText` / `tokenize` 回归。
- `test-host.mjs` `v0.28 RAG 索引重建：fileTreeBodyLoader 接入后 rag-build-index /
  search-rag 链路正常`：建项目写正文 → 镜像含最新正文 → `rag-build-index`
  （status=fresh、indexedChunks≥1）→ `search-rag` 命中文件树正文 chunk →
  `rag-status` fresh。

> 注：开发中曾尝试构造「revision 冲突时索引反映磁盘实况」场景，发现 `load()` 末尾
> 无条件 `mirrorFileTree()` 会把磁盘收敛回内存状态（同内容不重写），架构上不存在
> 「内存 ≠ 磁盘」的持久分歧窗口——故 RPC 测试落在「正常路径 + loader 等价性」，
> loader 优先/回退逻辑由 `test-rag.mjs` 纯逻辑覆盖。

## 待办（v0.29+ 候选）

- `[x]` RAG 索引重建：file-first 后索引构建直接读 `.md` → v0.28
- `[ ]` 回收站 UI：Studio 回收站视图（列出 / 恢复 / 清空，`trash-list` RPC 已就绪）—— v0.29 候选
- `[ ]` Studio 检索视图：命中结果点击跳转实体（`search-rag` 已返回结构化命中）—— v0.29 候选
