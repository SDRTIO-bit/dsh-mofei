# dsh-mofei v0.29.0 变更说明（回收站 UI：列出 / 恢复 / 清空）

> 2026-08-21。v0.27 的 `.mofei/trash/` 回收站只有文件层机制（删除实体移入回收站 + 只读
> `trash-list`）。本版补齐闭环：Host 新增 `trash-restore` / `trash-purge` RPC，
> Studio 新增「回收站」视图（活动栏 + 迷你导航 + 命令面板入口），误删可一键恢复。

## Host 变更（`plugin/lib/index.js`）

### `trash-restore { projectId, batch }` —— 恢复一个批次

- 把 `trash/<projectId>/<batch>/` 下的 `.md`（与同名 `.history.jsonl`）移回项目文件树
  （rootDir 项目同样支持），随后 `importFileTree()` 回填 store——**正文与历史一起回来**
  （v0.27 起实体历史在 `.history.jsonl`，随文件整体恢复）。
- **实体 id 冲突保护**：恢复的 frontmatter `id` 与现存实体相同 → 分配新 id、重写 frontmatter、
  换新文件名（`.history.jsonl` 同步换名），**绝不覆盖现存实体**。
- 章节 / 角色 / 笔记 / 世界书 / 摘要 / 链均支持（按相对路径目录推断 kind）。
- 全部文件移走后清理批次空壳目录；`trash-list` 跳过空批次（防残留显示）。
- 返回 `{ restored: [{ kind, id, name, newId? }] }`。

### `trash-purge { projectId?, batch? }` —— 清空回收站

- 三级粒度：批次（`projectId` + `batch`）/ 项目（仅 `projectId`）/ 全部（缺省）。
- 返回 `{ purged: 文件数 }`。

## Studio 变更（`plugin/src/client/legacy.js`）

- **活动栏**新增「♻ 回收站」按钮（笔记之后、设置之前）；**底部迷你导航**新增「回收站」；
  命令面板新增 `/mofei:trash`。
- 回收站视图：
  - 批次列表：项目名 + 删除时间 + 文件数，文件路径明细；
  - 每批次「↩ 恢复」（实体冲突时自动换新 id，恢复后自动刷新项目列表）与「× 删除批次」
    （两击确认）；
  - 顶部「刷新」与「清空全部」（两击确认）；
  - 错误 / 结果提示条；空态文案说明回收站语义。

## 验证

```powershell
node --check plugin/lib/index.js
node --check plugin/src/client/legacy.js
node plugin/esbuild.config.js           # 重新打包 lib/client.js
node test-host.mjs                      # 59/59（v0.29 新增 2 项 + 既有 57 项）
node test-rag.mjs / test-roles.mjs / test-role-host.mjs / test-mofei-writer-preset.mjs
node test-subagent-max-role.mjs / test-txt.mjs / test-world.mjs
node test-ai.mjs / test-summary.mjs / test-prompt-chain.mjs   # 全 PASS
```

v0.29 新增测试（`test-host.mjs`）：

- `v0.29 回收站：trash-restore 恢复删除的实体（含历史回填），trash-purge 清空`：
  删章节 + 角色 → 2 批次入回收站 → 恢复章节（正文 = 删除前最新版、`chapter-history`
  从 `.history.jsonl` 回填、无冲突保留原 id）→ 恢复角色 → 批次消失 → 再删再按项目
  `trash-purge` → 批次消失。
- `v0.29 回收站：trash-restore 实体 id 冲突时分配新 id，不覆盖现存实体`：
  手工构造与现存章节同 id 的回收站批次 → 恢复 → 分配新 id、frontmatter 重写（标题保留）、
  现存章节内容未被覆盖、新实体独立可读。

3088 实机闭环（临时项目，未触碰用户数据）：删除 → `trash-list` 批次可见 →
`trash-restore` 恢复（id 不变、内容完整）→ `trash-purge` 清空 → 批次归零。

## 待办（v0.30+ 候选）

- `[x]` 回收站 UI（列出 / 恢复 / 清空）→ v0.29
- `[ ]` Studio 检索视图：命中结果点击跳转实体（`search-rag` 已返回结构化命中，检索视图
  已存在但跳转待完善）—— v0.30 候选
- `[ ]` Prompt Chains 版本 diff（git log -p 呈现）—— v0.30 候选
