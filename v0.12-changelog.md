# dsh-mofei v0.12 变更说明（路线 B：墨扉成为官方 web 一等视图标签 + 质感修复）

> 生成：2026-08。基线 v0.11.0。
> 背景：用户反馈「墨扉 web 质感差、对话框变形、希望独立 web」。调研 DSH 官方 web 结构与社区定制实例（[ikun](https://www.npmjs.com/package/@wangleledev/dsh-client-ikun)、[dsh-skin](https://github.com/KinGao294/dsh-skin)、[Catppuccin-dsh-theme](https://github.com/zhijun-dai/Catppuccin-dsh-theme)、[deepseek-harness-desktop](https://github.com/ningbainb/deepseek-harness-desktop)）后，用户拍板路线 B。

## 新增 / 变更

### 1. 官方 web 一等视图标签（conversation.view，路线 B）

- 墨扉注册为官方 DSH web 的 **conversation.view 标签**（`slots.inject('conversation.view')` + `register({name, id:'mofei-studio', order:30, label:()=>'墨扉'})`）——这是官方支持的三方扩展点（官方对话 tab 本身即如此注册），社区已验证随 DSH 升级平滑。
- Studio 新增 **内联视图模式**（`mode='view'`）：无 overlay 包裹、填满官方视图区（`.mf-view`）、头部无关闭按钮（由官方标签环切换）、命令面板隐藏「退出墨扉」、挂载注入 CSS / 卸载移除（零残留）。
- 官方对话 tab 零改动——**对话质感由官方保证，不再有变形问题**；墨扉工作台成为官方 web 的一等标签（3088 即独立端口，打开即官方质感 web）。
- 侧栏入口与 overlay 形态保留（兼容既有回归，标记为 legacy）。

### 2. 对话框变形修复（官方样式护法对齐）

- 气泡：`max-width:min(525px,82%)`、圆角 22px、`overflow-wrap:anywhere`、`box-sizing:border-box`（官方对话气泡数值）。
- 输入卡：圆角 22px、`min-width:0` 防 flex 挤压、`flex:none` 按钮。
- 头部标题省略号、消息区 `overflow-x:hidden`、工具帧 `min-width:0`。
- 官方护法核心：flex 子项 `min-width:0` 阻断最小内容宽度（变形真因）。

### 3. 墨扉皮肤（可选，不自动应用）

- 注册 `ctx.theme.register({id:'mofei', colorScheme:'dark', tokens:{墨韵令牌集}})`——墨色底 + 米色文字 + 茶金品牌色；官方主题设置里可切换（社区范式：覆盖 `--dsw-alias-*` 令牌）。

### 4. 调研沉淀

- 官方 web 是插件化 client 装配；主题 = 令牌覆盖层（`--dsw-alias-*`，规范见官方 `docs/web-styling.zh.md`）；第三方视图/皮肤扩展点已验证可行。

## 验证结果

```text
verify-v0.12-view               header 标签环出现「墨扉」→ 点击 → 内联渲染（无 overlay/无关闭按钮）
                               → 活动栏/对话面板可用 → 切回对话标签视图卸载 ALL PASS
verify-v0.11-chat               对话面板（流式/工具帧）ALL PASS
浏览器 8 项回归                  ALL PASS（待终跑确认）
```

## 改动文件

```text
plugin/src/client/legacy.js     conversation.view 注册 + Workspace mode='view'（内联渲染/无关闭按钮/命令过滤/CSS 生命周期）
                               + 对话样式官方对齐 + theme.register('mofei')
plugin/lib/client.js            重建 client bundle
verify-v0.12-view.cjs           新增：视图标签浏览器验收
tools/verify-client-bundle.mjs  Workspace 签名匹配容错
plugin/package.json             version 0.12.0
```

## 备注

- 官方 header（含视图标签环）只在**非 blank 会话**渲染——新会话是 hero 引导态，发一条消息即出现标签。
- 视图标签随官方 conversation 契约演进；自绘 Studio 只依赖 slots/theme 公开扩展点与自有会话数据层。
- overlay 形态保留为兼容入口；新用户路径 = 官方对话 tab ↔ 墨扉工作台 tab。
