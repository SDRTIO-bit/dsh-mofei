# mofei-dsh v0.14.0 变更说明（变形金刚形态：原版 web + 墨扉气泡）

> 生成：2026-08-16。基线 v0.13.1。
> 背景：用户拍板新方向——「墨扉不是独立界面，是在原版 web 基础上加东西：
> 加气泡，不是喧宾夺主、不是另起炉灶、不是鸠占鹊巢。有一个按钮，点击，
> web 像变形金刚一样变形，墨菲的部件冒出来，而不是新 web 界面」。
> 本版推翻 v0.12.1–v0.13「3088 整体 = 墨扉 web（轨道压 0 + conversation.session 替换）」方案。

## 形态（两态变形）

### 默认态 = 原版 DSH web（完整保留，零覆盖）

- 官方侧边栏（280px：新会话/会话列表/搜索/设置）、官方对话、官方 composer（含空态 hero 引导卡）
  全部回归可见——不再压轨道、不再替换 conversation.session、不再隐藏 composer。
- 右下角一个圆形「墨」字浮动按钮（orb）。
- 墨扉面板完全收起在屏幕外，页面无全屏遮罩、无叠层。

### 点 orb → 同一页面平滑变形（0.32s 过渡）

| 部件 | 变形方式 |
| --- | --- |
| 官方侧边栏 | 用**官方原生折叠机制**收成 55px 窄条（logo/会话列表/设置图标保留，点击同步官方 toggle） |
| 官方对话 + 官方 composer | CSS 挤到右侧 **430px** 气泡（官方原部件变窄靠右，气泡自适应） |
| 墨扉工作台 | 从左侧滑入（55px 窄条右侧，宽 = 剩余空间）：左内栏（项目→章节两级导航 + 底部迷你导航）+ 编辑器（标签/标题/工具条/正文/状态栏） |

- 变形后 orb 退场，收起走墨扉顶栏「✕ 收起」；再点还原原版 web（侧栏展开、composer 回位、面板滑出）。
- 官方侧栏底部「墨扉」入口同样触发变形（双入口）。
- 空会话时官方输入 = hero 引导输入卡，变形后在 430px 内自适应（输入能力全程保留）。

### AI 写作联动（官方对话不可侵入，联动移到墨扉侧）

- 「送章/送选中」保留：结构化提及直接注入当前 DSH 会话（官方对话区上屏）。
- 新增「📄 跳转提及」：跳到 Agent 最新回复提及的章节。
- 新增「⌄ 插入回复」：把 Agent 最新回复插入正文光标处（助手回复完成后出现）。
- 自绘对话面板退役（右侧就是官方对话，不再重复造）；PendingCard 审批由官方 composer 原生承载。

## 其他

- 撤销：`[class*="_frame"]` 轨道压 0、`[data-composer-seat]`/composerHero 隐藏、conversation.session 单槽替换。
- 墨韵令牌调亮一档（label-tertiary #6b6b74→#8a8a92、caption、dimmed），官方侧栏与墨扉面板文字对比度修复。
- 样式修复：centerCol 与对话根之间隔着 `display:contents` 包装层，挤右规则用后代选择器。

## 验证结果（全绿）

```text
verify-v0.14-view.cjs       18 项：默认原版 web（侧栏280/composer1312/无遮罩/orb）→ 变形（侧栏55/
                            composer 438@x1154/面板滑入/迷你导航）→ 还原 + 侧栏入口双路径 ALL PASS
verify-v0.14-chat.cjs       9 项：官方输入框两态可用/送章送选中/跳转提及/官方窄条 6 图标 ALL PASS
verify-v0.14-t5.cjs         3 项：送章→官方对话出现提及→助手回复→「⌄ 插入回复」插入正文（真实回合）ALL PASS
verify-v0.14-workbench.cjs  7 项：变形→项目行→编辑器/标签/状态栏/风格/命令面板→还原 ALL PASS
verify-v0.14-isolation.cjs 21 项：3 轮默认完整（frame 未被压轨道/无遮罩/面板隐藏）+ 变形/还原干净 ALL PASS
单元测试 11 个文件           全 PASS
verify-shots/layout-geometry.cjs  两态几何（默认 frame 280/1320/0；变形 sidebar 55 + panel 滑入 + composer 438）
verify-shots/vision-look.cjs      vision 三态复核：默认态 clean、变形后三列结构确认
```

## 改动文件

```text
plugin/src/client/legacy.js      v0.14 全部改动：MofeiBubble/气泡状态/官方侧栏折叠同步/挤右 CSS/
                                 撤销替换与轨道压制/「✕ 收起」/插入回复与跳转提及/令牌调亮
plugin/lib/client.js             重建 bundle
plugin/package.json              version 0.14.0（node 写入，无 BOM）
verify-v0.14-*.cjs               5 个新回归脚本（替代 v0.10/0.11/0.12 时代断言）
verify-shots/capture-mofei.cjs   三态截图（default/transformed/workspace）
verify-shots/layout-geometry.cjs 两态几何
v0.14.0-changelog.md             本文档
```

## 备注

- 依赖官方哈希类名的选择器集中在三处（hHd-Xa_root/toggle 折叠同步、centerCol 挤右、composerSeat），
  随 DSH 升级需复核（语义段保留：_root/_toggle/centerCol/composerSeat/scrollBody）。
- 变形动画为 CSS transform/padding 过渡；官方侧栏折叠走官方机制（含官方自带过渡）。
- 旧 verify-v0.10/0.11/0.12/verify-t5 脚本断言的是「全屏墨扉」形态，已被 v0.14 系列取代，
  勿再运行（运行会 FAIL，属预期）。
