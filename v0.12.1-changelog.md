# mofei-dsh v0.12.1 变更说明（3088 整体 = 墨扉 web：conversation.session 整体替换）

> 生成：2026-08-16。基线 v0.12.0。
> 背景：用户对 v0.12 路线 B（官方 web conversation.view 标签）的**最终拍板修正**——
> 「3088 是被改造的 dsh web，不是加一个墨扉的标签」。即：打开 3088，**整体就是墨扉 web**
> （写作工作台 + Agent 对话 + 官方输入体验），不是「官方 web + 墨扉 tab」。
> 路线 B 的标签方案**废弃**，本版以 plan C 实现用户意图。

## 新增 / 变更

### 1. conversation.session 单槽整体替换（plan C，核心）

- 废弃 `conversation.view` 标签注册；改为 `slots.inject('conversation.session')` +
  `slots.register({name:'conversation.session', priority:-1}, (props) => Workspace({mode:'web'}))`
  ——single 槽整体替换官方会话视图，**blank 会话也显示工作台**（无 hero 引导、无标签环）。
- Studio 新增 **web 模式**（`mode='web'`）：`.mf-view-root` 根容器 + `.mf-panel.mf-view` 主面板，
  无 overlay 包裹、头部无关闭按钮、命令面板隐藏「退出墨扉」、CSS 挂载/卸载零残留。
- 官方 `ConversationRoot` 仍在：**底部官方 composer（composerSeat）保留**——
  输入、发送、工具审批、附件、模型选择全部走官方通道。

### 2. 右面板 Agent 对话（自绘 + 官方质感）

- 右面板自绘对话流：`chat-utils.normalizeChatItems` 渲染会话快照（用户/助手/工具帧/元信息），
  气泡样式沿用官方数值（`max-width:min(525px,82%)`、圆角 22px、`min-width:0`）。
- web 模式**移除面板内输入框**：输入与审批统一走底部官方 composer
  （面板在 pending 时提示「请在页面底部输入区处理」）。
- 已知取舍：官方对话组件无法嵌入右面板（DSH renderSlot 所有权限制——"not declared by this entry's children"），
  自绘方案是官方约束下的最优解；对话质感与输入体验由官方组件保证。

### 3. 墨韵皮肤自动应用（v0.12.1 补强：完整令牌 + 强制生效 + 浅色变体）

- 令牌补全：从 11 个扩展到**官方 design-platform.css 全部 `--dsw-alias-*` / `--dsw-specific-*`**
  （含气泡 `--dsw-specific-bubble`、输入卡 `--dsw-specific-input-major`、侧栏/菜单/滚动条/Markdown/状态/按钮全套）。
- 机制修正：`theme.setTheme('mofei')` 仅改内存偏好，会被设置层 `adopt()` 用持久化偏好（默认 system）覆盖——
  改用 **`theme.overrideTokens('mofei-dsh', {light, dark} 对)` 令牌叠加层**强制生效（与偏好无关，apply 清理时移除）。
- 新增**浅色变体「宣纸」**（MOFEI_PAPER）：米纸底 + 墨字 + 茶金，随用户颜色偏好自动切换；注册主题 `mofei` 仍保留在设置里可选。

### 4. 对话面板增强（T5：缩小版 DSH web 补齐）

- **审批/提问面板内直接应答**（`PendingWait.respond`）：右面板渲染 pending 卡片——工具审批（允许一次/拒绝，
  outcome `allowed-once`/`rejected`）+ 提问（选项按钮单选/多选、自由输入，answer `{id, selected, custom}`），
  与底部官方 composer 双通道并存，任一处应答即结算。
- **新建会话预设选择**：`agentPresets.list` 拉取可用预设，多预设时聊天头出现选择器（默认 mofei-writer），
  「＋」按所选预设创建并 select。
- **对话 ↔ 编辑器联动**：提及消息（送章/送选中/Writer/Reviewer 格式）显示「📄 跳转章节」
  （解析 `projectId:/chapterId:` 定位并打开章节）；助手回复完成显示「↓ 插入正文」（插入光标处）。

### 5. 布局修复（T5 测试揪出的真 bug）

- `.mf-body` 网格定义 8 列但 web 重构后只有 7 个子元素 → `.mf-chat` 一直落在 **6px 沟槽列**（面板实际不可见，
  此前测试断言宽松误报通过）。修正为 7 列（聊天面板自带左边框作分隔），并在
  verify-v0.12-view / verify-v0.11-chat 增加**宽度断言（>200px）**防回归。

### 6. 兼容路径保留

- 侧栏「墨扉」入口与 overlay 形态保留（legacy，不影响 web 模式；注意 web 模式下勿再点它，会叠一层 overlay）。

### 7. 视觉打磨（OpenFic 对比轮，vision 模型逐图审查驱动）

- **空态/占位符对比提升**：`.mf-empty` 行高/字号/对比增强；官方 composer 占位符由 coding 文案改为
  「输入写作指令：续写 / 审稿 / 查设定…」。
- **克制次要操作**：`.mf-mini`/`.mf-grid-btn` 从 11px 纯红小字升级为 12px 柔和色
  （`--dsw-alias-state-error-primary` + 危险 hover 底），padding/圆角/高度统一放大。
- **布局修复**：项目卡片 actions 允许换行、进度条 min-width、章节行标题 ellipsis 截断
  （修「重命名/删除挤压重叠」「长标题挤爆 minis」「树节点文字被裁」）；编辑区工具条按钮统一 24px 高。
- **官方杂讯清理**：隐藏官方 composerHero（「探索未至之境/预览版」等 coding 平台装饰，`[class*="composerHero"]` 语义类选择）；
  「Full access」模式徽章保留（功能信息）。
- **留白与字号**：活动栏/聊天头/状态栏 padding 与 gap 增大、项目卡标题 13→14px、footer 12px。
- 审查闭环：`verify-shots/vision-look.cjs`（调本地 `deepseek-v4-vision`，key=`1`）+ `capture-mofei.cjs`（三态截图）。
  对比结论：OpenFic「克制层级 + 充足留白 + 温和功能色」vs 墨扉旧版「信息堆叠 + 危色滥用 + 空态失明」——已全部对齐。

## 验证结果

```text
verify-v0.12-view.cjs      web 模式端到端 9 项：Studio 主区（无标签环/无 overlay）→ 活动栏 → 右对话面板
                           （含宽度断言 340px）→ 底部官方 composer → 发消息 → 用户气泡 + 助手流式回复  ALL PASS
verify-v0.11-chat.cjs      web 模式 12 项：唯一主面板/对话面板存在（含宽度断言）/官方 composer 通道/流式回复/
                           「＋」新建/预设选择器/无「×」折叠按钮/活动栏折叠展开  ALL PASS
verify-v0.10-isolation.cjs  web 改造 21 项：唯一主面板 3 轮回归/无叠层/无浮动/CSS 注入  ALL PASS
verify-v0.10-workbench.cjs  web 改造 7 项：项目网格/编辑器/标签/状态栏/风格/命令面板  ALL PASS
verify-t5-linkage.cjs        T5 联动 4 项：提及「跳转章节」/回复「插入正文」/跳转打开章节/插入光标处  ALL PASS
verify-skin-snapshot.cjs     墨韵令牌采样：bubble #2f2b26 / input-major #26231f / brand #b09a76 等全部生效
单元测试 11 个文件          全 PASS（chat-utils 6、agent-bridge 29、其余见 HANDOFF-WSL.md §1）
```

## 改动文件

```text
plugin/src/client/legacy.js     conversation.session 单槽整体替换 + Workspace mode='web' 分支
                               （.mf-view-root/.mf-panel.mf-view/无关闭按钮/命令过滤/聊天面板 web 分支）
                               + MOFEI_INK/MOFEI_PAPER 双色板（完整令牌）+ theme.overrideTokens 强制生效
                               + 右面板自绘对话 web 渲染 + PendingCard（审批/提问面板内应答）
                               + 新建会话预设选择（agentPresets.list）+ 提及跳转/回复插入正文
                               + .mf-body 网格 8→7 列修复（聊天面板 340px）+ 气泡 flex 纵向布局
plugin/lib/client.js            重建 client bundle
verify-v0.12-view.cjs           重写：web 模式端到端验收 + 聊天面板宽度断言
verify-v0.11-chat.cjs           web 化改造（官方 composer 通道）+ 聊天面板宽度断言
verify-v0.10-isolation.cjs      web 化改造（唯一主面板 3 轮回归）
verify-v0.10-workbench.cjs      web 化改造（RPC 种子后 page.reload() 刷新 bootstrap）
verify-t5-linkage.cjs           新增：T5 对话↔编辑器联动冒烟（提及跳转/插入正文/预设）
verify-skin-snapshot.cjs        新增：墨韵令牌采样诊断（改皮肤后跑）
plugin/package.json             version 0.12.1
```

## 备注

- 验证脚本已全部 web 化且**含布局宽度断言**：v0.10-isolation 21 项 / v0.10-workbench 7 项 /
  v0.11-chat 12 项 / v0.12-view 9 项 / t5-linkage 4 项 —— **5 脚本全绿**。
- 权威叙事切换：NEXT-SESSION.md §1/§11/§12/§13 已同步改写为「3088=墨扉 web」；交接文档见 `HANDOFF-WSL.md`。
- 收官仍待**用户真实使用验证**（NEXT-SESSION.md §13 清单）。
