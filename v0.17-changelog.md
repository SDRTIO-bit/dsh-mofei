# mofei-dsh v0.17.0 变更说明（技能开关/自创技能入口 + 会话导航改进）

> 生成：2026-08-16。基线 v0.16.1（commit `14fbb77`）。
> 需求来源（用户反馈）：
> 1. 墨菲技能槽 17 个技能不知有何用、怎么用；想要 OpenFic 式技能开关 + 自创技能入口；
> 2. 墨菲内找不到退出当前对话的入口，也找不到历史会话记录（必须先退出墨菲去官方 DSH 选会话）。
> 计划与方案：`PLAN-v0.17-skills-sessions.md`。

## 功能

### 技能开关（OpenFic 式）

- 写作技能面板（「✦ 技能」）每项技能带启用开关（✓ 已启用 / ○ 已禁用）；
- 开关写入 `.mofei-skill-settings.json`（disabledSkills 名单），`mofei-dsh/skills` 技能插件注册时按名单过滤
  ——关闭的技能对写作助手完全不可见（不再出现在 skill 工具候选里）；
- 说明：技能注册发生在写作会话的 preset 组合时，开关改动**下次新建写作会话时生效**（面板与 RPC 均有提示）。

### 自创技能入口

- 面板「＋ 新建技能」表单：技能名（小写 kebab-case）/ 描述 / 适用场景 / 技能正文；
- 创建即写入 `~/.dsh/skills/<name>.md`（frontmatter 对齐 DSH skill-filesystem 规范：
  `name` / `description` / `whenToUse` + 正文），**DSH 原生自动发现**，写作助手 skill 工具即可加载；
- 面板顶部统计「N 项内置能力 · M 项自创」，自创技能在列表底部单独分组展示。

### 会话导航改进

- 顶栏「写作助手」会话菜单升级为两区：
  - 当前项目写作会话（原样：打开 / ＋ 新建本项目会话）；
  - **全部会话**：官方会话列表（最近 30 条，按活跃时间排序），每条显示
    `✍`（mofei-writer）/`·`（standard）徽标 + 标题 + 时间，**点击即切换打开**（subagent 会话自动跳过）；
- 「**退出当前对话**」按钮：解除右侧 Agent 面板的会话绑定，回到会话选择态；
- 命令面板新增：`/mofei:sessions`（打开会话列表）、`退出当前对话`。

## 验证结果（全绿）

```text
verify-v0.17-skills-sessions.cjs  10 项 ALL PASS：
  技能开关（禁用→落盘→查询 enabled:false→恢复）
  自创技能（创建→~/.dsh/skills/<name>.md 落盘→custom 列表可见→清理）
  会话菜单（「全部会话」区 / 26 条历史会话项 / 退出按钮存在）
  退出当前对话（绑定解除，菜单回到会话选择态）
verify-v0.14-view.cjs             回归 ALL PASS（抽查）
```

## 改动文件

```text
plugin/lib/index.js                skill settings 存储 + list-skill-settings / set-skill-enabled /
                                   create-custom-skill / delete-custom-skill RPC + mofeiService.listSkillSettings
plugin/lib/skills-plugin.js        注册前按 disabledSkills 过滤（注入 mofei 服务）
plugin/src/client/skills-library.js  面板：技能开关 + 新建技能表单 + 自创技能分组
plugin/src/client/legacy.js        接线技能面板 + 会话菜单升级（全部会话/退出当前对话）+ 命令面板
plugin/lib/client.js               重建 bundle
plugin/package.json                version 0.17.0
verify-v0.17-skills-sessions.cjs   新增端到端回归
PLAN-v0.17-skills-sessions.md      计划文档
v0.17-changelog.md                 本文档
```

## 备注

- 自创技能目录 `~/.dsh/skills/` 是 DSH skill-filesystem 的用户技能根（与本机所有 DSH profile 共享）；
- 技能开关只影响 mofei-writer preset 内注册，standard 会话本来就没有这些技能（隔离不变）；
- 会话列表数据来自官方 `sessions.list` 快照订阅，删除/归档仍由官方会话管理负责。
