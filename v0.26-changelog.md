# dsh-mofei v0.26.0 变更说明（删除镜像修复 + 文件优先彻底落地）

> 2026-08-21。本版把 NEXT-SESSION.md §3「P0：文件优先闭环」从「双写」真正收口为「JSON 只留索引」，
> 并把未提交代码里那批删除 bug（镜像文件未清、reload 后复活）的修复合并进来。

## 核心变更

### P0：JSON 只留索引（`.mofei-projects.json` 不再含正文/描述/历史）

| 字段 | 原位置（v0.25） | 新位置（v0.26） |
| --- | --- | --- |
| `chapter.content` | `.mofei-projects.json` + `.mofei/projects/<id>/chapters/<id>.md`（双写） | 仅 `.mofei/projects/<id>/chapters/<id>.md` |
| `chapter.history[]` | `.mofei-projects.json` | 仅 `.mofei/projects/<id>/chapters/<id>.history.jsonl`（JSONL 顺序追加） |
| `character.description` | `.mofei-projects.json` + `characters/<id>.md` | 仅 `characters/<id>.md` |
| `note.content` | `.mofei-projects.json` + `notes/<id>.md` | 仅 `notes/<id>.md` |
| `world-entry.content` | `.mofei-projects.json` + `world/<id>.md` | 仅 `world/<id>.md` |
| `chain.content` | `.mofei-chains.json` | 仅 `.mofei/projects/<id>/chains/<id>.md` |

JSON 文件现在只保存「索引 + 元数据」：

```text
.mofei-projects.json   — projects → { id, title, description, goal, currentStyle, writerSessionId,
                                       chapters[] { id, title, order, revision, volumeId },
                                       volumes[], characters[] { id, name, isFavorited },
                                       notes[] { id, title, categoryId, isLocked, isHidden },
                                       noteCategories[], worldEntries[] { id, name, keys,
                                       isEnabled, constant, order } }
.mofei-chains.json     — byProject[projectId] → [ { id, name, updatedAt } ]（无 content）
```

实现：

- `toIndexableStore()` / `toIndexableChainStore()`：save 前深拷贝并 `delete` content/history 字段；
  version 字段保持 v4 / v1（读取路径不破）。
- `mirrorFileTree()`：章节多写一个 `<id>.history.jsonl`，每条快照 `{ revision, content, at, source? }`。
- `importFileTree()`：reload-from-files 时一并解析 `<id>.history.jsonl`，回填 `chapter.history`。
- 删除链路（`removeMirrorEntityFiles`）：删章节时同步把旁边的 `.history.jsonl` 一并 `rm`。

### P0：删除 bug 修复（合并此前未提交的改动）

所有 `delete-*` RPC（`delete-project` / `delete-chapter` / `delete-volume` /
`delete-character` / `delete-note` / `delete-world-entry` / `delete-world-entries` /
`delete-prompt-chain`）都接上：

- `removeDeletedMirrorEntities(project, { chapters, characters, notes, worldEntries,
  chapterSummaries, rangeSummaries, chains })`：按 frontmatter.id 精确定位并删除镜像 .md。
- `removeProjectMirror(project)`：默认项目目录整目录 rm；rootDir 项目只清 `manifest` + `project.yml`，
  绝不递归删除用户选定的外部小说文件夹。
- `.mofei-manifest.json`：记录墨扉生成的相对路径，清理时只按清单遍历，rootDir 下用户手写 Markdown 不会被误删。

测试 `真实文件树删除实体后镜像文件清理且 reload 不复活` 已覆盖章节 / 卷 / 角色 / 笔记 / 世界书 / 链 / 项目共 7 类删除 + `reload-from-files` 不复活 + 手工 Markdown 不被牵连。

## 升级不变量

1. **不破坏旧数据**：旧 `.mofei-projects.json`（version 4，带 content）依旧能 load——in-memory store 仍含完整 content/history；下一次 `saveProjects()` 自动剥离正文，落到文件树。
2. **rollback / chapter-history 仍可用**：rollback 用的是 in-memory `chapter.history`，save 时落盘到 `.history.jsonl`；reload 后再读回。
3. **并发安全不变**：所有写操作仍走 `mutate()` 串行队列；`expectedRevision` 冲突保护照旧。
4. **git 自动提交不变**：`.mofei/` 内的实体文件改动继续被 `gitCommitAll('墨扉 项目保存')` 节流提交。
5. **章节正文上限不变**：编辑器 10 万字 / 2000 行限制照旧（在 UI 层校验，不在 RPC 层）。

## 兼容性矩阵

| 客户端能力 | 旧 JSON（含 content） | 新 JSON（无 content） |
| --- | --- | --- |
| `list-projects` | 返回完整 chapterView（含 content） | 同左（in-memory 有 content） |
| `read-chapter` | 返回完整 chapter | 同左 |
| `chapter-history` | 返回 history 列表 | 同左（从 `.history.jsonl` 重建） |
| `rollback-chapter` | 按 revision 回退 | 同左（in-memory history 来源换成文件树） |
| 直接读 `.mofei-projects.json` | 看到完整正文 | 只能看到元数据（需读 `chapters/<id>.md`） |
| 直接读 `.mofei/projects/<id>/**` | 文件树 | 文件树（v0.26 起正式成为唯一正文来源） |

## 验证

```powershell
node --check plugin/lib/index.js
node test-host.mjs                       # 54/54（v0.26 三项新单测 + 既有 51 项）
node test-roles.mjs                      # ROLE CATALOG PASS
node test-role-host.mjs                  # ROLE HOST PASS
node test-mofei-writer-preset.mjs        # MOFEI WRITER PRESET CONTRACT PASS
node test-subagent-max-role.mjs          # SUBAGENT ROLE RESOLUTION PASS
node test-txt.mjs                        # 9/9
node test-world.mjs                      # 7/7
node test-ai.mjs                         # 7/7
node test-summary.mjs                    # 10/10
node test-prompt-chain.mjs               # 15/15
```

新增三项测试（`test-host.mjs` 末尾）：

- `v0.26 文件优先：saveProjects 落盘后 .mofei-projects.json 不含 chapter.content/history/...`：写两份正文 + 多次 update-chapter，断言 JSON 不含 content/history，`.mofei/projects/<id>/**` 内 `chapters/<id>.md` 含最新版正文、`<id>.history.jsonl` 含旧版快照。
- `v0.26 文件优先：第二实例从文件树回填 chapter.content/character.description/note.content/world.content`：模拟 DSH 重启后新实例跑 `read-chapter` / `list-projects`，断言 content / description / world.content 从 `.md` 回填到 in-memory。
- `v0.26 文件优先：delete-chapter 同步清理 .history.jsonl；reload 不复活历史`：删章节后 `.md` + `.history.jsonl` 同步消失，`reload-from-files` 不复活。

## 待办（v0.27+）

- `[x]` 删除 bug 修复 → v0.26
- `[x]` JSON 不含 content / history → v0.26
- `[ ]` 实体历史（角色 / 笔记 / 世界书）的 history/snapshot 同样落到文件树（目前仍在 JSON）—— v0.27 候选
- `[ ]` 删除章节时给 `.history.jsonl` 加「回收站」目录，避免误删后无法恢复—— v0.27 候选
- `[ ]` RAG 索引重建：file-first 后索引构建可省略 chapter.content 拉取，直接读 `.md`—— v0.28 候选