# mofei-dsh v0.24.0 变更说明（Host 生命周期修复 + 行为参数可配）

> 纯修复版本：默认行为与 v0.23 完全一致，无新功能开关。

## P0：Host 生命周期（卸载/热重载不再泄漏）

- `lib/index.js` 此前丢弃 `ctx.webServer.register` 返回的 disposer，
  卸载/热重载后 `/api/mofei`、`/api/openfic`、`/mofei` 路由残留，
  重装配会因 duplicate prefix route 崩溃。v0.24：
  - 三处路由统一经 `safeRegister` 收集 disposer；
  - `apply` 末尾 `ctx.effect` 注册清理：路由注销 + `gitCommitTimer` 清除 +
    `agentContexts` 清空；
  - 历史版本残留路由导致的重复注册只告警、不崩溃（兼容升级过渡）。
- 说明：`jobs.attachController` 由 `dsh-jobs-local` 以 `layers.effect` 绑定调用方，
  自动 detach，无需处理。

## P1：行为参数可配（cordis.yml）

- 新增 `normalizeCoreConfig(config)`（与 subagent-max 同款手动归一化，零新依赖）：
  `historyCap` / `entityHistoryMax` / `gitCommitIntervalMs` / `rag.*`
  （chunkSize / chunkOverlap / candidateLimit / resultLimit / confidenceThreshold）。
- 缺省值 = 原硬编码值（20 / 50 / 10000ms / rag 默认），不配即行为不变。
- RAG 调用改为 `{ ...coreConfig.rag, ...(单次 RPC 参数) }`：
  插件配置为基底、单次调用可覆盖。
- `~/.dsh/profiles/novel/cordis.patch.yml` 附注释配置样例。
- 完整 schemastery `Config` schema（校验 + 设置 UI 表单）留作 v0.25
  （需引入 `@deepseek-ai/schemastery` 运行时依赖）。

## 验证

- 新增回归测试 `tests/verify-p0-lifecycle.mjs`（mock ctx）：
  路由注册/清理、服务提供、重复注册容忍、虚拟根目录下服务可调用、自定义配置兼容。
- 热重载实测：路由存活、无 duplicate route 崩溃。
