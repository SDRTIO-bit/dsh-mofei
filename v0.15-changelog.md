# mofei-dsh v0.15.0 变更说明（文件同步轮询：AI 写入 → 墨扉自动可见）

> 生成：2026-08-16。基线 v0.14.1（commit `752dce9`）。
> 背景：用户实测反馈「AI 已经写入文档，但是墨菲看不见，没有进行轮询」。
> 计划与方案：`docs/archive/PLAN-v0.15-sync.md`。

## 问题根因（已定位）

墨扉此前只有一条「AI 写入 → UI 刷新」通路：监听**已绑定写作会话**的聊天工具完成事件
（`legacy.js` 正则匹配 `mofei_/openfic_` 写类工具名）。以下场景全部漏刷：

1. AI 用未绑定会话 / 其他流程写入（工具名不匹配、事件被会话历史压缩、气泡关闭期间写入）；
2. 外部编辑器直接改 `.mofei/projects/**/*.md`（文件优先闭环 P0 目标，此前需手动 `reload-from-files`）；
3. 气泡关闭期间发生的外部编辑，重新打开工作台不可见。

## 功能

### Host：轻量 RPC `sync-status`（`plugin/lib/index.js`）

- 返回双签名，客户端分别响应：
  - `storeStamp`：内存 store 的章节 revision 指纹（工具/UI 写入后内存已新，只需 UI reload）；
  - `fileStamp`：`.mofei/projects/**` 文件+目录 mtime 混合签名（`count:max:sum`，外部编辑时需文件优先导入）；
- **TTL 缓存 1200ms**：2s 轮询基本命中缓存，不做全量磁盘扫描；
- 与只读 RPC 一致走 `load() + queue` 串行队列，virtual-root 环境返回空签名。

### Client：工作台 2s 轮询（`plugin/src/client/legacy.js`）

- Workspace 挂载期持续轮询（气泡开/关都工作，打开即最新）：
  - `storeStamp` 变化 → 仅 `reload()`（AI 工具写入）；
  - `fileStamp` 变化而 `storeStamp` 未变 → `reload-from-files`（文件优先导入）+ `reload()`（外部编辑）；
  - 首轮执行一次 catch-up：补上「气泡收起期间」发生的外部写入；
- busy 防重入 + `timedCall` 超时保护（fetch 悬挂不卡死轮询）+ alive 清理；
- `reloadRef` 每次渲染更新，轮询永远调用最新闭包；
- 既有聊天工具事件刷新保留（双保险，可即时响应）。

### 修复：镜像写入 ping-pong 循环

`mirrorFileTree()` 此前**无条件重写全部文件**（内容相同也写）→ 每次保存刷新所有文件 mtime →
轮询误判「外部编辑」→ `reload-from-files` → 镜像 → 每 2s 全量镜像 + git add 的无限循环
（探针实测 fileStamp 每 ~2s 变化）。已改为 `writeMofeiFile` **内容相同跳过写**，从根源消除 mtime 噪声。

### 冲突与草稿保护（沿用现有机制，未新增）

- 作者本地有未保存草稿时 AI 写入 → `reload()` 保留草稿并置冲突提示（「写作 Agent 已更新远端正文，当前草稿没有被覆盖。」），
  编辑器提供「保留草稿继续 / 使用远端正文」操作；
- 用户自己保存触发的签名变化：revision 已被同步 → reload 无副作用；
- `reload-from-files` 遵循「文件 revision ≥ store revision → 文件胜出」；
- `gitCommitAll` 有 10s 节流 + 「nothing to commit」短路，无空提交。

## 验证结果（全绿）

```text
verify-v0.15-sync.cjs  9 项 ALL PASS：
  场景A 模拟 AI 工具写入（纯 RPC）→ 编辑器 ≤10s 自动可见（轮询生效）
  场景B 外部改章节文件（revision+1）→ 文件优先自动回读
  场景C 本地未保存草稿 + AI 再写入 → 草稿不被覆盖 + 冲突提示可见
verify-v0.14-view.cjs      回归 ALL PASS
verify-v0.14-workbench.cjs 回归 ALL PASS
```

## 改动文件

```text
plugin/lib/index.js           sync-status RPC + storeStamp/fileTreeStamp(TTL) + writeMofeiFile 内容相同跳过
plugin/src/client/legacy.js   2s 轮询 effect + timedCall 超时包装 + reloadRef
plugin/lib/client.js          重建 bundle
plugin/package.json           version 0.15.0
verify-v0.15-sync.cjs         新增文件同步轮询端到端回归
docs/archive/PLAN-v0.15-sync.md            计划与验收文档
v0.15-changelog.md            本文档
```

## 备注

- 轮询期间若 `sync-status` 出错会静默跳过（下轮再试），不打断工作台；
- 不做 fs.watch（Windows/网络盘可靠性差），不做存储改造；
- 实测注意：验证脚本会在工作区创建/删除 v15 测试项目并产生「墨扉 项目保存」自动提交（既有副作用，历史一致）。
