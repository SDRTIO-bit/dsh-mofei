# mofei-dsh v0.3.1 变更说明（改码完成，待下次重启验证）

> 基线：v0.3.0（世界书 / chapter-context / TXT 编码检测 / 13 个 Agent 工具）。
> 数据兼容：新增独立文件 `.mofei-ai-sessions.json`；旧数据无需迁移。

## 新增

1. **AI 会话历史**
   - 按项目持久化到 `.mofei-ai-sessions.json`（`version:1, sessions[projectId].messages[]`）。
   - 消息：`{ id, role: user|assistant, content, mode, at }`，上限 80 条，单条 ≤16000 字。
   - `ai-assist` 自动把最近 8 条历史（可 `maxHistory` 调）与本次 user 消息一起发给 LLM，并落盘 user+assistant。
   - RPC：`ai-history` / `ai-clear-history`。
   - Client：AI 面板新增「会话历史 / 清空」，显示最近 6 条。
2. **批量章节摘要**
   - RPC：`ai-summarize-chapters`，可选 `chapterIds`，按 order 排序，最多 30 章，顺序生成 150 字摘要。
   - Client：AI 面板新增「批量摘要」按钮，结果显示在面板内。
   - 失败时返回 `{ error, partial, total }`，保留已完成部分。
3. **AI 调用修复**
   - 统一 `generateText()`：DSH content-block 消息（`[{type:'text',text}]` + plugin source）+ 读取 terminal `finish` 的 error/aborted。
   - `ai-assist` 先 `load()+queue`，修复冷启动直接调用时项目未加载的问题。
4. **Agent 工具 13 → 16 个**
   - 新增：`mofei_get-ai-history` / `mofei_clear-ai-history` / `mofei_summarize-chapters`。
   - 注册前自动 sanitize 工具名（非法字符 → `_`），不合规直接跳过注册。
5. **写作技能完整迁移：16 → 17 个 runtime skills**
   - 来源：OpenFic-main `backend/app/skills/*.yaml` 全量解析（含 `references` 原始方法论）。
   - `tools/migrate-skills.mjs` 生成 `plugin/lib/skills.js`（可复跑；源 YAML 更新后重跑即可）。
   - Host 启动时经 `ctx.skills.register()` 注册：`mofei-character-design` / `mofei-character-relationship` / `mofei-deslop-lexicon` / `mofei-deslop-writing` / `mofei-dialogue-design` / `mofei-emotional-arc` / `mofei-opening-design` / `mofei-prose-format` / `mofei-reader-contract` / `mofei-reversal-design` / `mofei-short-submission` / `mofei-story-deconstruction` / `mofei-story-hooks` / `mofei-story-quality` / `mofei-story-state-tracking` / `mofei-villain-reveal` + 原有 `mofei-writing`。

## 改动文件

```text
plugin/lib/ai.js        新增：会话/批量摘要纯逻辑（test-ai.mjs 单测）
plugin/lib/skills.js    新增：17 个 runtime skills（由 tools/migrate-skills.mjs 生成）
tools/migrate-skills.mjs 新增：技能迁移生成器
plugin/lib/index.js     AI 会话持久化 + ai-history/clear/summarize + 16 工具 + 17 skills + generateText
plugin/lib/client.js    AI 面板：历史/清空/批量摘要
plugin/package.json     v0.3.1 + files 含 lib/ai.js / lib/skills.js
test-ai.mjs             新增：6 项纯逻辑单测
test-host.mjs           13 项（新增历史/清空/批量摘要/skills 注册集成测试）
```

## 验证清单（下次 DSH 重启后）

- [ ] `test-txt` 9/9 · `test-world` 7/7 · `test-ai` 6/6 · `test-host` 13/13
- [ ] AI 面板「会话历史」可见 0 → 生成一次 → 2 条 → 清空 → 0
- [ ] AI 面板「批量摘要」返回每章摘要
- [ ] `Tool.listTools` 可见 16 个 `mofei_*`
- [ ] 会话内 `/mofei-story-quality` 或 skill 工具能加载 17 个 mofei-* skills
- [ ] 真实 `ai-assist` 不再是 `LLM_EMPTY` / `LLM_FAILED`（除非模型未配置）

## 回退

- 删除 `.mofei-ai-sessions.json` 即清空 AI 历史，不影响项目数据。
- 移除 `index.js` 末尾 skillsService 注册块即可关闭 17 个 runtime skills。
- 如需回退到 v0.3.0：移除 `index.js` 中 v7 AI handlers / 3 个新工具 / `lib/ai.js` import 即可。
