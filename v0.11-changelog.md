# dsh-mofei v0.11 变更说明（DSH 对话交集：Studio 内嵌缩小版 DSH web）

> 生成：2026-08。基线 v0.10.3。
> 核心理念落地：墨扉 = DSH（agent 对话与能力）× 小说写作。v0.11 把「DSH 的 agent 对话」直接搬进写作工作台。

## 新增 / 变更

### 1. 右侧常驻 Agent 对话面板（缩小版 DSH web）

- **位置**：Studio 右侧新增常驻对话列（`.mf-chat`，默认 340px），Activity Bar 新增「💬 对话」按钮切换开合；专注模式与移动端自动隐藏；折叠时网格自动回收列宽。
- **绑定**：自动绑定 DSH 当前会话（`sessions.list` 订阅，随主界面切换会话同步）；头部显示会话标题与 agent 预设（如 `standard` / `mofei-writer`）。
- **消息流**：基于官方会话快照（ConversationSnapshot）自绘渲染——
  - 用户/助手气泡（user/assistant/steering 节点）
  - **流式输出**：`partial` 节点实时渲染（「写作 Agent 正在输入…▌」），完成后替换为最终消息
  - **工具调用展示**：assistant 消息内的 `tool-call` 块（⚙ 工具名）、运行中调用帧（⏳ runningCalls）、工具结果（✔/✖ + 摘要）、命令行（/命令 完成/出错）、压缩/错误等元信息行
  - 未知节点类型安全跳过（kind 判别式，DSH 事件扩展不崩）
- **输入**：Enter 发送 / Shift+Enter 换行；回合进行中可「停止」（session.cancel）；审批等待显示提示（到 DSH 主界面处理）。
- **新建会话**：「＋」按钮 → `sessions.create` → 尝试 `agentPresets.select({ sessionId, agentPreset: 'mofei-writer' })`（仅 blank 会话，失败回退默认预设）→ 设为当前会话。
- **架构**：不依赖官方 conversation UI 组件（调研确认：`dsh-client-modules` 的 makeRequire 禁止跨插件 value import，官方组件无法复用）；纯数据层 API（`ctx.get('sessions')` 的 list/binding + 快照订阅），不受 UI 包演进影响。
- **纯逻辑抽取**：`plugin/src/client/chat-utils.js`（快照→渲染项折叠，含 6 项单测）。

### 2. 调研结论（docs/OPENFIC-RESEARCH.md 之外新增）

- 官方对话组件复用被模块加载器硬禁（`dsh-client-modules/lib/client.js` makeRequire 拒绝跨插件 value import）；会话 UI 全在 slot 注册闭包内，无受支持路径嵌入自绘 overlay。
- 会话数据层 API 稳定：`sessions.list.getSnapshot()/subscribe`、`sessions.binding(id).session`（ObservableSnapshot：`subscribe/getSnapshot` → `{nodes, partial, runningCalls, running, pending}`）、`prompt(content,'queue'|'steer')`、`cancel()`、`sessions.create({})`、`agentPresets.select({sessionId, agentPreset})`（blank 会话限定）。

## 验证结果

```text
chat-utils.test              6/6（快照折叠/流式 partial/运行中工具帧/未知节点安全）
verify-v0.11-chat            绑定当前会话 + 用户上屏 + 助手流式回复（真实 LLM「你好墨扉」）+ 折叠/展开 ALL PASS
test-host                    47/47
浏览器 8 项回归               ALL PASS（待终跑确认）
```

## 改动文件

```text
plugin/src/client/legacy.js         右侧对话列（网格/折叠/移动端）+ 会话绑定/发送/停止/新建 + 面板渲染
plugin/src/client/chat-utils.js     新增：快照→渲染项纯逻辑
plugin/src/client/chat-utils.test.mjs  新增：6 项单测
plugin/lib/client.js                重建 client bundle
verify-v0.11-chat.cjs               新增：对话面板浏览器验收
plugin/package.json                 version 0.11.0
```

## 备注

- 对话面板与主 DSH web 共享同一会话状态：在 Studio 发消息 = 在当前会话发消息；「＋」新建会话会切为主界面当前会话（交集语义，可随时切回）。
- 已知边界：审批（approval/question）在面板内仅提示，需到 DSH 主界面处理（v0.11 范围外）；会话历史消息在会话窗口内渲染，极长会话受官方窗口截断策略约束。
