# dsh-mofei v0.14.1 变更说明（写作状态：让 AI 会话进入 mofei-writer）

> 生成：2026-08-16。基线 v0.14.0。
> 背景：用户需求「如何让 AI 会话进入写作状态」。
> 机制（dsh 官方 API，已读源码确认）：写作状态 = 会话挂 `mofei-writer` agent preset
> （墨扉 36+36 写作工具 + 17+17 写作技能只注册在该预设的 agent plane，standard 会话零污染）。

## 功能

### 工作台顶栏「写作状态」徽标 + 智能进入按钮（变形后可见）

- 徽标实时显示当前 DSH 会话状态：`✍ 写作中`（mofei-writer）/ `○ 空白会话` / `· 标准会话`。
- 「✍ 进入写作状态」按钮，智能切换：
  1. **空白会话** → `agentPreset.select({ sessionId, agentPreset: 'mofei-writer' })`
     原地 recompose（不换会话，当前会话直接变写作会话）；
  2. **已开聊的标准会话**（官方锁定预设，历史在旧工具集下产生）→
     `session.create({ agentPreset: 'mofei-writer' })` 新建写作会话并打开；
     空白切换若被服务端判 locked 也自动降级为新建；
  3. **已是写作会话** → 提示「已是写作会话 ✍」。
- 切换成功后右窄条（官方对话+输入）即为写作 Agent 通道，直接下写作指令即可。

### dsh 官方机制要点（实现依据，随版本升级需复核）

- `POST /api/agentPreset.select`：仅 **blank 会话**（事件流无 `turn/start`）可 recompose；
  已开始会话返回 `agent-preset-locked`（官方注释：历史是在旧预设工具集下产生的）。
- `POST /api/session.create`：payload 直接支持 `agentPreset`，创建即写作会话（响应回显 `agentPreset`）。
- 客户端 RPC 信封：`{ type: 'client-request', rpcId, method, payload }`。

## 验证结果（全绿）

```text
verify-v0.14-writing.cjs    7 项：官方 API 空白会话原地切换 mofei-writer /
                            session.create 直接建写作会话 / 徽标「✍ 写作中」/
                            按钮存在 / 重复点击提示 / 还原 ALL PASS
verify-v0.14-view.cjs      18 项 ALL PASS（回归）
verify-v0.14-chat.cjs       9 项 ALL PASS（回归）
verify-v0.14-t5.cjs         3 项 ALL PASS（回归，真实回合）
verify-v0.14-workbench.cjs  7 项 ALL PASS（回归）
verify-v0.14-isolation.cjs 21 项 ALL PASS（回归）
单元测试 11 个文件           全 PASS
```

## 改动文件

```text
plugin/src/client/legacy.js      写作状态徽标/按钮（mf-head）+ enterWritingMode（select/新建降级）+
                                 徽标 CSS（.mf-wstate）
plugin/lib/client.js             重建 bundle
plugin/package.json              version 0.14.1
verify-v0.14-writing.cjs         新增写作状态回归
v0.14.1-changelog.md             本文档
```

## 备注

- 交付测试中当前开发会话已被切换为 mofei-writer（功能真实生效的证明；该会话 blank 判定为 true，
  标准编码工具全部保留，仅增加写作工具/技能）。
- 会话预设一旦 commit（首轮 turn/start 后）即锁定——这是官方设计，勿试图绕过。
