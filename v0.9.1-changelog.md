# mofei-dsh v0.9.1 变更说明（novel 首次启动验收修复）

> 生成：2026-08-15。基线：v0.9.0 全绿但尚未在重启后的 novel Host 上验收。
> 本版完成 NEXT-SESSION.md 中唯一未完成项：novel profile 首次启动、运行时验收、真子代理写作流水线。

## 本版修复

1. **Agent 工具 output schema 仍有 `type` 数组（4 个工具注册失败）**
   - `mofei_read-chapter` / `openfic_read-chapter` 的 `volumeId: { type: ['string','null'] }`
   - `mofei_get-chapter-summary` / `openfic_get-chapter-summary` 的 `entry: { type: ['object','null'] }`
   - DSH 0.1.0-rc.6 的 schema 子集不支持 type 数组；改为 `oneOf`：
     `volumeId: { oneOf: [{type:'string'},{type:'null'}] }`，
     `entry: { oneOf: [{type:'object'},{type:'null'}] }`。
   - 修复后 novel 3088 启动日志无任何 `tool register failed`。

2. **Agent 工具 render 签名错误（关键阻塞）**
   - 旧代码：`renderText = (value) => ...`。
   - DSH 运行时调用约定是 `output.render(args, value)`；旧签名把输入 args 当成 value 渲染，
     导致模型调用任何 `mofei_*` 工具都只看到“输入参数回显”，看不到业务数据。
   - 修复：`renderText = (_args, value) => ...`。
   - 这是此前“工具注册成功但子代理流水线跑不通”的根因。

3. **`verify-v0.8.cjs` 新 Host 路径脚本 bug**
   - `list-projects` 返回 RPC 信封 `{ok,value}`，旧代码直接解构 `{ projects }`。
   - 旧 Host 走 SKIP 分支所以从未暴露；novel 重启后暴露并修复为 `listResult.value.projects`。

4. **`test-host.mjs` 契约增强**
   - 迷你 JSON Schema 校验器新增 `oneOf` 支持。
   - 新增 render 签名回归：`output.render(args, value)` 必须渲染 value，禁止渲染 args。

## 新增验收工具

```text
tools/verify-agent-surface.mjs        DSH /api 协议建临时 session，核对 17 mofei-* + 17 openfic-* skills
tools/verify-agent-tools.mjs          真实 agent 统计自身可见工具：46 = 23 mofei_* + 23 openfic_*
tools/verify-subagent-pipeline.mjs    真子代理 Writer→Reviewer 流水线（建临时项目→subagent 续写→subagent 审稿→回读校验→清理）
```

用法（先启动 novel）：

```powershell
$env:MOFEI_BASE = 'http://127.0.0.1:3088'
node tools\verify-agent-surface.mjs
node tools\verify-agent-tools.mjs
node tools\verify-subagent-pipeline.mjs
```

## 启动命令勘误（v0.9.0 交接文档）

```text
# dsh 0.1.0-rc.6 下会报 “web takes none of parent --profile”：
dsh --profile novel web --port 3088   # 错误

# 正确写法（不需要显式写 web）：
dsh --profile novel --port 3088

# novel profile 首次启动前需要安装一次链接依赖：
dsh plugin --profile novel install
```

## 验证结果（3088 新 Host，全绿）

```text
启动日志:        无 tool register failed
HTTP:            /api/mofei /api/openfic bootstrap OK；client.js 200；/mofei/ 200
单测:            test-host 46/46；其余全部 node 单测通过
流水线冒烟:      tools/verify-writing-pipeline.mjs ALL PASS
浏览器回归:      verify-v0.6 / v0.7 / v0.8 / v0.9 / v7 / v5-ui 全部 ALL PASS
Agent 面:        skills 17+17；agent 可见 tools 46（23 mofei_* + 23 openfic_*）
真子代理:        Writer -> {"status":"done","revision":3}
                 Reviewer -> {"verdict":"PASS"}（青城/林轩/剑意 全部命中）
                 章节字数 325，revision=3，临时项目已删除
```

## 改动文件

```text
plugin/lib/index.js                   oneOf schema ×2 + renderText(args,value)
plugin/package.json                   version 0.9.1 + description
test-host.mjs                         迷你校验器 oneOf + render 回归
verify-v0.8.cjs                       list-projects 信封解构修复
tools/verify-agent-surface.mjs        新增
tools/verify-agent-tools.mjs          新增
tools/verify-subagent-pipeline.mjs    新增
v0.9.1-changelog.md                   新增
```
