# mofei-dsh v0.10.3 变更说明（收官打磨轮）

> 生成：2026-08。基线 v0.10.2。收官前全面打磨：审计、diff 视图美化、自动刷新、发布产物。

## 新增 / 变更

### 1. Agent 写工具审计（history 来源标记）

- 实体/章节 history 条目新增 `source` 字段：`'agent'` = 经 Agent 工具写入，缺省（null）= UI/RPC。
- `tools.js` 的 `run()` 包装自动注入 `_source: 'agent'`；`mofei_write-character` / `mofei_write-world-entry` 修复为走同一包装（此前直连 mofei.run 绕过审计）。
- 覆盖的写路径：章节（update/edit/rollback）、角色、笔记（含移动）、世界书（含批量/移动/回滚）。
- `chapter-history` / `entity-history` RPC 透出 `source`；Studio 实体历史面板显示来源标记。
- 验收：test-host 新增「Agent 工具写入的 history 条目带 source=agent（UI/RPC 无 source）」47/47。

### 2. Studio git diff 视图美化

- patch 从 `<pre>` 升级为逐行着色渲染：`+` 绿 / `-` 红 / `@@` 蓝 / commit·Author·diff 头等灰。
- CSS：`.mf-git-diff` / `.mf-diff-add` / `.mf-diff-del` / `.mf-diff-hunk` / `.mf-diff-meta`。

### 3. Jobs 摘要完成后自动刷新

- AI 面板「批量摘要」走 Jobs 完成后，自动调用 `refreshSummaryPanel()`（摘要面板数据即时更新，无需手动刷新）。

### 4. 论证性关闭两项（写入决策记录）

- **Jobs 覆盖导出 TXT / 索引重建**：关闭。两者均为 <2s 短任务，不符合 DSH Jobs 适用场景（每 owner 并发上限 + 管道开销，见 docs/OPENFIC-RESEARCH.md 与 R1 调研结论），维持同步实现。
- **检索 RRF 混合接口预留**：关闭。墨扉 retrieve 已为未来替换留好接口（单一 RPC + 结构化输出），等 DSH 向量/embedding 生态成熟再引入 RRF，无需现在占位。

### 5. 发布产物

- `plugin/dist/mofei-dsh-0.10.3.tgz`（npm pack，15 个文件，385KB）。
- 修复 package.json `files` 清单遗漏 `lib/summary.js` / `lib/prompt-chain.js`（此前打包产物会缺依赖无法加载）。

## 验证结果

```text
test-host                          47/47（新增审计测试）
verify-spec / retrieve-style / git-history / jobs  ALL PASS
verify-agent-isolation/tools/surface / subagent-pipeline  ALL PASS
agent-bridge.test                 29/29
浏览器 8 项回归                     ALL PASS（待最终复跑确认）
verify-client-bundle               CLIENT BUNDLE CONTRACT OK
```

## 改动文件

```text
plugin/lib/index.js                history source 标记（pushHistory/pushEntityHistory + 视图透出 + writeSource）
plugin/lib/tools.js                run() 注入 _source='agent'；write-character/world-entry 走统一包装
plugin/src/client/legacy.js        git diff 行着色 + 实体历史来源显示 + Jobs 完成自动刷新
plugin/package.json                version 0.10.3 + files 补 summary.js/prompt-chain.js
plugin/dist/mofei-dsh-0.10.3.tgz   发布产物
test-host.mjs                      新增审计单测
tools/verify-client-bundle.mjs     knownExternal 补 setInterval
```

## 收官状态

- 原交接文档（NEXT-SESSION.md §12）完成定义全部勾选；本版本为收官候选版。
- **待用户实际使用验证后正式收官**：见 NEXT-SESSION.md 附「实测验证指南」。
- 剩余 backlog（不阻塞收官）：Agent 写工具审计的批量/链路径深化、Studio diff 单提交选择、摘要 mid/far 段位配置化。
