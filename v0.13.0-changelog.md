# mofei-dsh v0.13.0 变更说明（布局定稿：中=文字区，右=缩小版 dsh web）

> 生成：2026-08-16。基线 v0.12.1。
> 背景：用户对「3088 整体 = 墨扉 web」的布局最终拍板（经 4 轮 HTML 预览确认）——
> 「墨菲是 DSH web 的扩展，将 web 缩小至右边气泡，不喧宾夺主」。
> 最终形态：**中间 = 墨菲文字展现与修改区；右边 = 缩小版 dsh web 气泡（官方侧边栏条 + 对话 + 输入一体）**。

## 新增 / 变更

### 1. 布局定稿（HTML 预览驱动，用户逐轮确认）

- **中**：文字展现与修改区（一个气泡）——左内栏 264px（项目 → 章节**两级导航**：项目列表 →
  选中项目后切换为卷/章节列表，← 返回；底部迷你导航 项目/检索/角色/世界/笔记）+ 右内栏编辑器（标签页/标题/工具条/正文/状态栏）。
- **右**：缩小版 dsh web（一个气泡，三段一体）：
  - **官方会话条**（非驻留）：默认只显示「‹ 会话列表」方向键；点击弹出会话列表（`sessions.list`，
    标题 + 相对时间 + 预设），选择/新建后自动收起回到方向键；
  - **官方质感对话**：22px 圆角深灰气泡（用户=特定 bubble、助手=hover 底）、⚙ 工具帧、预设选择、新建会话、PendingCard 审批；
  - **官方 composer 缩小版**：输入框移入右气泡（`binding.prompt`），底部通栏官方 composer 隐藏。
- **官方列处理**：官方 frame 轨道压成 `0 minmax(0,1fr) 0`（**不能用 display:none**——grid auto-placement
  会让 centerCol 掉进第一列 280px 轨道导致整页塌缩，实测踩坑）；官方侧边栏/详情列/底部 composer 全部退场。

### 2. 两级导航（web 模式）

- `tab==='projects'` 且已选项目 → 左内栏显示章节视图（← 返回 + 项目名 + 搜索章节 + 新建章节 + 卷分组/章节树）；
  未选项目 → 项目网格（ProjectGrid）。overlay 模式保持旧两列布局不变。

### 3. 会话条交互

- `selectChatSession(id)`：`sessions.open` 切换 + 收起列表；「＋ 新会话」复用 `newChatSession`（含预设选择）。

### 4. 视觉（沿用 OpenFic 对比轮的纯黑中性 + 蓝 accent 体系）

- 无暖棕、灰阶文字、唯一 `#4d8dff` accent；气泡 16px 圆角 + 细边框 + 深投影；面板间距 16px。

## 验证结果

```text
verify-v0.12-view.cjs      13 项：主区渲染/无标签环/无 overlay/左内栏+mini-nav/右气泡 402px/
                           官方 composer 隐藏/右气泡输入框/发消息/流式回复  ALL PASS
verify-v0.11-chat.cjs      12 项：唯一主面板/右气泡 402px/面板内输入框/会话条（方向键→弹出→选择收起）/
                           流式回复/「＋」新建  ALL PASS
verify-t5-linkage.cjs      4 项：提及跳转/插入正文（输入改走右气泡） ALL PASS
verify-v0.10-workbench.cjs 7 项  ALL PASS
verify-v0.10-isolation.cjs 21 项：迷你导航替代活动栏断言  ALL PASS
单元测试 11 个文件          全 PASS
verify-shots/layout-geometry.cjs  布局几何断言（官方轨道 0 / panel 全宽 / col 264 / editor flex / chat 402）
verify-shots/vision-look.cjs      vision 复核：三栏结构、迷你导航、右气泡输入框全部确认
```

## 改动文件

```text
plugin/src/client/legacy.js     web 模式布局重排：mf-body flex 双区（左内栏+编辑器 / 右气泡三段）
                               + 两级导航（← 返回章节视图）+ mini-nav + 会话条（方向键/列表/选择）
                               + 输入框移入右气泡 + 官方轨道压 0 / composer 隐藏
plugin/lib/client.js            重建 client bundle
verify-v0.12-view.cjs           更新：composer 断言 → 右气泡输入框 + 官方 composer 隐藏断言
verify-v0.11-chat.cjs           更新：会话条交互断言 + 面板内输入框
verify-t5-linkage.cjs           更新：输入定位改右气泡
verify-v0.10-isolation.cjs      更新：迷你导航替代活动栏
verify-shots/capture-mofei.cjs  更新：两态截图（项目视图/章节视图）
verify-shots/layout-geometry.cjs 新增：布局几何断言
verify-shots/layout-diag.cjs     诊断脚本（保留）
plugin/package.json             version 0.13.0
```

## 备注

- 布局经用户 4 轮预览确认（docs/archive/preview-mofei-v2.html 保留为设计稿参照）。
- 官方 frame 轨道覆盖用 `!important` 对抗内联样式；哈希类名选择器依赖语义段（`_frame`/`sidebarCol`/`detailsCol`/`composerHero`），
  随 DSH 升级需复核（见 docs/archive/HANDOFF-WSL.md §6 已知坑）。
- 收官仍待**用户真实使用验证**（NEXT-SESSION.md §13）。
